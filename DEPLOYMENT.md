# Linux 账户交易管理

前端和交易服务部署在同一台 Linux 服务器，由 `aster-desk` systemd 服务运行，同时提供公共额度提醒和交易完成汇总。无需 Windows 定时任务。支持 XAUUSD1、SPCXUSD1、CLUSD1，最多 8 个独立账户。代码具备实盘执行入口；部署本身不会创建或启动实盘账户。

## 页面状态刷新与成交统计

余额、仓位、账户启停和执行进度继续随页面状态刷新。成交量、成本和最近 100 笔明细由单个后台任务读取本地账本，完成后间隔至少 5 秒再更新，多页面共享结果；统计慢时不会让状态请求排队等待。网页会标明统计截至的 UTC 时间，首次读取显示等待，超过 15 秒、UTC 换日或更新失败时保留上次值并提示待更新。切换统计品种或修改成交额度上限后，旧设置的结果不会混入新设置。

统计使用独立的 SQLite 只读快照，不占用交易写入锁，不增加交易所请求。下单时的额度核对继续直接读取最新持久账本，后台显示结果不参与发单判断。升级仍使用下方一键安装命令，无需新增依赖或配置。

## 有额度开仓交易对

“普通开仓 → 编辑普通开仓参数 → 有额度开仓交易对”可选全部交易对，或仅 XAUUSD1、SPCXUSD1、CLUSD1 中的一个。先暂停账户并等待当前批次完成，再选择、保存并启动账户。设置按账户保存，重启后保留，旧账户默认全部交易对。仅所选品种参与普通开仓及升杠杆；该品种额度不足时等待，不自动改开其他品种。已有仓位继续计入账户风险，未完成订单继续核对和补救；成交量循环与仓位迁移仍按各自设置执行。

## 安装与访问

推荐 Ubuntu 24.04 或 Debian 12，要求 systemd、Python 3.10+，能访问 GitHub、npm、PyPI、nodejs.org 和 Aster。安装脚本会补充系统依赖、Python 虚拟环境和构建所需的 Node；Node 官方下载包经过 SHA256 校验。

```bash
curl -fsSL https://raw.githubusercontent.com/hxx344/aster_5x/main/install-trading.sh | sudo bash
```

已有仓库时可以执行 `sudo bash install-trading.sh` 安装本地代码。首次安装会打印随机生成的网页访问密码，并保存到 `/etc/aster-desk/environment`（root 读写）。服务默认只监听服务器 `127.0.0.1:8765`。

在自己的电脑建立 SSH 隧道，再打开浏览器 `http://127.0.0.1:8765`：

```bash
ssh -N -L 8765:127.0.0.1:8765 YOUR_SERVER_USER@YOUR_SERVER_IP
```

使用域名时，在环境文件设置 `ASTER_PUBLIC_ORIGIN=https://你的域名`，用现有 HTTPS 反向代理转发到 `127.0.0.1:8765`，保留 Host 并传递 `X-Forwarded-Proto`。例如 Caddy：

```caddy
aster.example.com {
    reverse_proxy 127.0.0.1:8765
}
```

## 接入实盘子账户

每个子账户有三项不可更改的固定前提：**全仓保证金模式、双向持仓模式、单币保证金模式（USD1）**。程序只读取核验，不提供修改这些模式的配置项、界面或操作。任一项不符时禁止启动；运行中检测到不符则持久化暂停该账户，保留未完成批次，并停止新开仓、升杠杆和补偿下单。模式恢复后仍需手动启动策略，已有批次可继续核对。网页逐项保留最近账户快照中的核验结果：通过显示“上次核实通过”，失败保持红色“不符合要求”，尚无结果显示“尚未核验”；快照日期、时间和经过时间单独显示，达到 8 秒、连接异常或时间异常另作提示，不会因等待刷新而抹去结果。页面结果不代表当前下单授权，交易前仍按原规则核验模式与快照时效。已有仓位允许多空数量差不超过 0.1%；超过数量差上限的标的等待人工核对，不升杠杆或加仓。程序不查询外部挂单，接入前由操作者清理外部委托，并避免其他程序或人工同时改变该账户仓位。

在服务器执行 `sudoedit /etc/aster-desk/environment`，填写每个账户在 Aster Pro API 页面提供的 user、signer、API agent 私钥。这里的私钥是 API 签名账户私钥，不是主钱包私钥。不要把密钥提交到 Git 或粘贴到网页。

```dotenv
ASTER_ALLOW_LIVE=1
ASTER_A_USER=0x账户地址
ASTER_A_SIGNER=0xAPI签名地址
ASTER_A_PRIVATE_KEY=0xAPI签名私钥
ASTER_B_USER=0x另一个账户地址
ASTER_B_SIGNER=0x另一个API签名地址
ASTER_B_PRIVATE_KEY=0x另一个API签名私钥
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/机器人地址
FEISHU_SIGN_SECRET=机器人的可选签名密钥
```

保存后执行 `sudo aster-desk restart`。登录网页，添加账户，模式选“实盘”，环境变量前缀填 `ASTER_A` 或 `ASTER_B`。账户默认暂停；读取成功后可检查实际持仓、杠杆、USD1 总权益和保证金占用率，再点击启动。修改服务器密钥需要重启服务。

新增账户后若读取失败，持仓数值错误会注明品种、方向、字段和不符合要求的数值，例如 `XAUUSD1 LONG 开仓价（entryPrice）必须为正数（收到 0）`；资产与风控档位错误也会注明对应字段。该提示表示账户数据校验失败，需要按具体字段排查；旧版只显示“数值必须为正数”时，先使用下方升级命令更新，再查看该账户的新提示。更新诊断提示不会自动启动账户。

持仓接口的标记价若返回 `0` 或 `0E-8`，账户读取会使用该品种的新鲜行情标记价补充，多空两侧统一使用同一价格，保留全部已有仓位及其风险占用。行情缺失、过期或仍为零时不发布该次账户快照，等待有效行情；负数、格式异常及账户与持仓不一致仍会报错。补充行情只发生在账户快照读取阶段，不在循环行情触发至下单的路径中增加 HTTP 查询。

每个账户的“普通开仓”页支持额度阈值、每批每边的金额上限（默认 10,000 / 1,000 USD1）和最低开仓杠杆（仅 5x / 10x / 20x，默认 5x）；“账户 → 账户基础风险设置”统一管理基础风险上限（保证金占用率，大于 0%、不超过 100%，默认 50%）。普通新增开仓在实际杠杆为 10x 或 20x 时，共同获得基础上限之上的额外 5 个百分点，最高 100%；5x 仍使用基础上限。迁移开仓的三档统一规则见下文。先暂停策略，等待未完成批次核对结束，再修改并保存，重新启动后按新设置执行；保存配置本身不调整杠杆。设置按账户持久化，升级和重启后保留；旧账户沿用原基础上限，未设置最低杠杆时补为 5x。历史最低杠杆不在支持列表且不超过 20x 时，向上迁移到最近的支持档位；超过 20x 时暂停账户，需在网页选择支持档位并保存后再启动。降低最低杠杆不会触发降档，历史持仓仍按真实杠杆展示与计算风险。

有多个符合条件的市场时，先选择可用开仓杠杆较高的市场；同档位再选择 BBO 相对价差 `(ask-bid)/mid` 较低的市场，只有档位和价差都相同时才按轮换顺序选择。例如 XAU 10x 与 SPCX 5x 都满足条件时，优先 XAU 10x。每批重新比较，需要先升杠杆的市场按本次目标档位参与排序，仍需确认升档后才能开仓；开仓须满足额度阈值、冷却、最小委托与原有风控条件。比较复用本轮取得的盘口和公共额度缓存，执行前再检查报价时效与额度。

新开仓固定每批每边至少 **500 USD1 名义金额**，按标记价格计算，多空合计至少 1,000 USD1；仍须满足交易所更高的最低金额和数量步长。风险、余额、深度或额度只允许更小数量时等待，不会凑单突破限制。设置中的每边上限允许 500–1,000,000 USD1，默认 1,000；数量步进和报价差异可能使上限恰好为 500 时无法形成有效批次。旧配置低于 500 的上限保留，但不能新开仓，重新启动前须先修改。旧小额批次的核对、撤单和必要补偿不受新开仓下限限制。

最终仓位由风险、余额和额度决定；单批金额只是拆单上限，不是总持仓目标。模拟账户采用独立账本，不向交易所下单，其成交不发送交易汇总通知。公共额度提醒使用网页保存的账户额度阈值；整个服务以 `--demo` 运行时不发送公共额度提醒。

面板顶部随所选账户显示“账户保证金比率”和“总名义持仓金额”：前者为 USD1 维持保证金除以面板显示的账户总权益，后者为该账户全部 USD1 仓位的 `|数量| × 标记价格` 之和，多空分别累加，不除以杠杆，也不只统计当前选中的交易标的。权益为零或负值时，比率显示 `—`。原“保证金占用率”仍用于策略风险约束；新增指标复用同一次账户快照，不增加交易所请求。

### 删除账户

在“账户”页底部点击“删除账户”，确认账户名称和标识后删除。需先暂停账户；未完成订单、成交后核对或尚未结束的循环持仓会阻止删除，并显示原因。尚未配置凭据、读取失败的暂停账户也可以删除，无需先连接交易所。

删除仅停止工作台对该账户的管理，不会平仓、撤销交易所 API 或移除服务器环境变量。账户配置、成交和执行历史保留在本地账本，已删除账户不再出现在选择列表中，也不占用 8 个账户名额。为避免新账户混入旧账本，重新添加须使用新的账户标识，可复用原凭据环境变量前缀。删除后自动切换到其他账户；全部删除后显示添加入口，重启不会恢复已删除账户。

## XAU 仓位迁移

账户下方的“XAU 仓位迁移”默认关闭。先暂停账户并等待当前批次核对结束，再保存迁移设置，启动账户后开始检查。保存开关本身不会启动账户，也不会开启服务器实盘权限。迁移模式开启期间停止普通新增开仓；即使 XAU 已清空，也继续保持这个限制，直到手动关闭迁移模式。关闭后再开启会创建新的迁移周期。

默认目标深度价差上限 **5 bp**、每批每边上限 **1,000 USD1**、每边成交金额误差 **5%**。源标的固定为 XAUUSD1，目标只允许 SPCXUSD1 和 CLUSD1。目标必须有足够的 5x 公开额度，再检查实际开仓档位额度、账户容量和保证金；不套用普通策略的额度提醒阈值。满足条件的目标按本批实际委托数量扫深度计算价差，选择较低者，完全相同时优先 SPCX。迁移与面板共用经过序号校验的 WS 原始订单簿，但迁移只接受最多 **3 秒**的有效快照，不能用面板 1 万 / 5 万金额档的展示值代替。

深度数量与金额使用精确累计值查询，保留完整数量网格和各项风控约束。搜索期间按源、目标深度中较旧者的剩余有效期停止过期计算；墙钟与单调计时共同限制寿命，系统时钟调整不会延长搜索。连接断开或更新序号失效会撤销该代已返回的旧快照，返回方案前仍再次核验有效性与时效。提交目标开仓前，重新读取最新共享深度、BBO 和账户快照，按当前额度完整重算；数量或杠杆发生变化时等待下一轮重新规划。

目标实际杠杆不得低于迁移起始 XAU 杠杆、当前 XAU 杠杆、目标已有杠杆和账户最低开仓杠杆。需要升档时先调整并重新读取确认，再重新评估交易条件；只使用 5x / 10x / 20x，禁止降低已有杠杆。先开目标多空、确认真实成交和数量平衡，再平对应 XAU 多空，所以需要临时可用保证金。迁移开仓无论使用 5x、10x 还是 20x，临时风险上限均为账户基础上限加 5 个百分点，最高 100%；三档、所有目标和批次共用这 5 个百分点，不重复叠加。例如基础上限为 93% 时，迁移三档均按 98% 检查。占用计算包含未平 XAU，不预支其保证金，并预留四腿手续费及盘口成本；目标 5x 公开额度门槛和实际开仓档位额度检查保持不变。空间不足时缩批或等待。

金额误差逐方向计算：`abs(目标保留开仓金额 - XAU 实际平仓金额) / XAU 实际平仓金额`。目标保留金额按本批实际开仓均价和补偿后保留数量计算；同时限制累计差额，后续批次优先抵消已有偏差。各标的内部的多空数量容差仍为 0.1%。普通迁移批次每边至少 500 USD1，尾批及避免留下尾仓的最后两批允许低于该应用下限，但仍满足交易所数量步长和最小委托条件。无法成交的尾仓显示剩余数量和原因，只有 XAU 多空都实际归零且本批已核对才显示完成。

每个批次使用独立、持久化的四侧仓位记录。结果未知的订单只查询，不重新提交原单；部分成交只在本批预算内减持 XAU 或本批新增目标仓位，不能动目标旧仓，也不会重新开 XAU。运行或核对期间可点击“停止迁移并暂停账户”，原子关闭迁移并暂停账户，已经提交的批次继续核对和必要减仓；批次结束后也保持暂停，普通策略不会自动恢复。其他参数仍要求暂停并完成当前批次后修改。发现实仓与回执不符、杠杆被外部改变或修复达到上限时暂停，页面显示待核对原因；“重新核对”保留原订单与成交记录。重启后沿用同一批次恢复，完成记录与累计金额在同一事务中保存。迁移完成或回退后的风险检查保存迁移用途，重启或关闭迁移开关后仍沿用本批迁移上限。

## 交易口径与执行

- 额度监控、升档目标和新增开仓仅支持 5x、10x、20x，每个标的三档共用一次额度采样。**最低开仓杠杆仅限制新增开仓，不阻止升档**，无论空仓还是已有持仓。只选择高于实际杠杆且额度满足要求的支持档位。未触发 10x / 20x 优先通道时，例如最低设为 10x、实际为 1x，可先升到 5x 并保留旧仓，确认达到 10x 后才允许新增开仓。实际杠杆处于不支持的档位时保留真实读数，等待可用的更高支持档位；不会按该档位监控额度或新增开仓。**全局禁止降杠杆**，已经处于更高杠杆时不会重置到最低档。历史未完成批次继续核对和必要补偿，旧档位的杠杆调整不会重新发送。
- 每批多空等数量，盘口价差 `(ask-bid)/mid ≤ 0.0005`。双向开仓和单腿补偿均提交 `MARKET` 市价单，不发送限价或 `timeInForce`；开仓两腿批量提交。BBO 用于下单前的价差、深度和数量估算，实际按交易所成交均价记账，每轮完成后重新读取账户。普通实盘轮次至少等待 10 秒，多账户按请求预算延长并错峰；暂停且没有待核对工作的账户至少 60 秒刷新。未完成订单继续优先核对，通常等待 5 秒再检查，接口退避时按要求延长。
- 各标的分别按 `|多头数量 - 空头数量| / max(多头数量, 空头数量) ≤ 0.001` 判断持仓平衡，允许恰好 0.1%；两边都为零视为平衡，只有一边非零不符合条件。该容差用于已有仓位的升杠杆、加仓和成交后的总持仓检查。新增委托仍按每边相同数量提交，实际成交量分别记录和汇总。
- 每个仓位的占用保证金为 `|数量| × 标记价格 ÷ 该仓位实际杠杆`。账户全部 USD1 仓位的占用分别累加，多空不抵消，不同标的按各自杠杆计算。保证金占用率为 `总占用保证金 / (crossWalletBalance + crossUnPnl)`，分母为 USD1 全仓钱包余额加全仓未实现盈亏，不使用账户顶层 USDT 汇总字段。维持保证金不再作为开仓上限指标。
- 普通策略分别查询账户余额与持仓；循环直接使用同一账户响应中的余额和全部持仓。采用账户报告、全部仓位报告合计、按仓位数量与标记价计算的浮盈亏中的较低值，并同步扣减可用余额中的额外亏损；循环若补查持仓接口，其报告的合计亏损也参与保守比较。不使用尚未确认的盈利增加开仓预算。重复持仓、重复 USD1 资产、无效实际杠杆或接口间持仓数量不一致都会停止本轮操作。
- 数量差、价差、步长和占用率的交易判断使用精确计算；显示值舍入不会放宽交易边界。下单数量同时满足一般数量和市价数量过滤器，市价最低名义金额按标记价格计算。外部数值最多 128 个字符，十进制指数绝对值最多 100，超出范围拒绝处理。
- 普通新增开仓每笔同时考虑两边手续费、价差及标记价格偏差对成交后总权益的影响，并检查账户档位容量、可用余额、最小数量和盘口深度。预测成交后的保证金占用率必须**不超过该批实际杠杆对应的账户上限（基础默认 50%；10x / 20x 默认 55%，允许恰好等于上限）**；接近上限时自动缩小每批数量，无法容纳最小一笔时等待。例如基础上限 90%、仅 XAU 持仓已占用 90% 时，SPCX 或 CL 确认到 10x / 20x 且满足额度等条件后可开仓，成交后整个账户占用最多 95%。额外 5 个百分点在 10x / 20x、所有标的和批次之间共用，不累加；基础设为 95% 及以上时，高杠杆上限最多 100%。每次成交后再读取账户核对实际占用率；超过本批适用上限时持久化暂停账户交易。本批标的与杠杆随完成或补偿结束记录原子保存，未完成批次及重启后的检查沿用同一规则；实际杠杆已不在 10x / 20x 时回到基础上限，旧版本没有批次信息的检查标记也沿用基础上限。启动时已有占用达到或超过上限，仍可在满足原额度条件时先升杠杆释放占用；升杠杆本身不新增仓位。升档成功后按确认的实际杠杆重算已有仓位占用及适用上限，只有预测成交后不超过该上限才能新增仓位。
- 无论空仓还是已有持仓，都会检查更高的可用档位，当前档仍有额度不会阻止升档。普通调度选择高于当前杠杆且满足条件的最低可用档位，可以跳过没有额度的中间档；10x / 20x 额度唤醒的优先任务先按 10x、20x 顺序选择可用的更高档，没有合适高档时再按普通规则评估。目标档公开剩余额度必须严格大于配置阈值和多空总持仓名义价值，账户档位也必须容纳现有仓位。例如未触发高档优先时，最低开仓杠杆设为 5x，XAU 当前 1x、5x 满足条件，会先升到 5x，读取账户确认成功，再按 5x 额度计算和分批加仓。
- 10x / 20x 的可用状态变化时，若公开额度严格大于已启用账户的网页阈值（升档还需超过缓存中的多空持仓总名义价值），立即优先唤醒该账户，跳过普通轮次间隔和错峰等待；持续可用不会每次采样都重复唤醒；同账户多个标的的机会合并排队，保留尚未处理的标的。高档调整提交后立即安排核对，确认成功后立即尝试一次首批开仓；已在可开仓高档的账户可直接进入开仓评估。执行时重新检查账户、实际杠杆、额度、盘口及全部风控，原有接口退避、本地预算和开仓冷却仍生效。暂停账户不会因额度出现而自动启动。
- 多档同时可用时，升档确认后先在该实际档位尝试开仓一批，再检查下一次升档；高档新机会可以优先于低于 10x 的待开仓批次；10x / 20x 自身的升档后首批顺序保留。跨市场仍遵守高杠杆优先，该顺序会保留到重启后。若当前档额度已不足，或风险、余额等不足以容纳最小一笔，则允许继续寻找可用的更高档，仍需满足原有开仓风控条件。没有更高可用档位且当前档在支持列表时，使用当前档额度；只有较低档有额度时等待，绝不降档。提交杠杆调整前再次读取实际杠杆，旧降档批次保留并暂停核对，不会重发。
- 使用多空总名义价值检查容量，不把相等多空抵消成零。额度为公开估算，再结合认证账户风控档位；交易所仍可能因为实际占用或行情变化拒单。

默认每分钟总预算为 1,800 权重，其中 300 保留给订单核对、撤单和补偿；剩余 1,500 中默认再为额度监控和公共档位更新保留 93，普通执行（包括优先开仓）的准入上限为 1,407。每新增一个正在循环的高频标的，监控预估增加 270 权重/分钟；同标的多账户共享，不重复增加。监控可用至 1,500，不能借用核对保留；普通执行也不能借用监控保留。交易所额度较低时同步缩减，监控保留最多占普通预算的三分之一。账户调度按执行准入上限扣除公共请求预估计算普通轮次，常规公共请求预估 **333 权重/分钟**，包含额度与公共档位更新 93、盘口 REST 回退 120、深度最坏重建预估 120；每个循环高频标的另加 270；健康 WS 深度读取不消耗 REST 权重。迁移先按实际需要重建的标的预检深度权重，健康时为 0，不再固定要求额外 60；选出方案后，另行预检强制核验模式的账户快照与批量下单 5 权重。所有实际请求仍逐笔检查共享预算，并结合交易所返回的同 IP 用量执行退避，不降低请求权重或放宽快照时效。

已移除全账户挂单 `/fapi/v3/openOrders` 和手续费率 `/fapi/v3/commissionRate` 查询；外部挂单状态不再检查，快照以 `open_orders: null` 表示未查询。程序自己的未完成批次仍按已保存的订单 ID 查询、撤单和补偿。手续费风险计算使用固定 **0.04%** 的预估值，实际扣费仍由交易所决定并反映在后续账户权益中。完整批次复用终态回执和实际仓位核对之后的账户快照进行成交后风控，不会复用开仓前快照或补偿前快照。

“本地执行请求预算不足”或“本地普通请求预算不足”由程序在发送前生成，记录为黄色等待事件，不代表 Aster 拒单。日志显示估算用量、适用上限、本轮所需权重、本进程计入量和 Aster 同 IP 回报量；后者来自 `X-MBX-USED-WEIGHT-1M`，可能包含同 IP 的其他进程，详见 [Aster 限流说明](https://asterdex.github.io/aster-api-website/futures-v3/general-info/#ip-limits)。这些数值用于排查预算来源，并非其他进程用量的精确拆分。实际收到 HTTP 429/418 或交易所限流代码时，仍记录 Aster 限流错误并按退避时间等待。同一账户的预算等待日志每分钟最多新增一次。

收到有效的响应时间与用量头后，本地预算对齐 Aster 的分钟窗口；同一分钟内较低或乱序的计数不会退还预算，上一分钟的迟到回包不会重新占满新窗口。在途及结果不明的请求保留保守计数，时间头无效时回退本地窗口。账户读取和升档前的预算预检结合即将到期的缓存估算费用，每个实际请求仍再次原子检查。

升杠杆前，执行器重新核验全账户与固定模式；该次快照完成后 1 秒内、且仍满足原有 8 秒时效时，可供同一账户的 API 适配层使用一次，减少一轮重复读取。快照过期、来源不匹配或直接调用适配层时重新读取；调整后仍读取实际账户确认结果。同一标的一秒内的盘口读取合并，并在每次使用时核验原有 3 秒时效，刷新失败不会回退旧报价。账户余额和持仓不在交易前后共享缓存。

调度按首次连接或可能升档 300、普通运行 120、运行中迁移 300、暂停 90 权重预估每轮成本；迁移共享 WS 深度后保留账户及交易复核的开销，深度重建另计入公共请求预估。新账户错峰进入，点击启动或重新核对会唤醒对应账户。三个市场的额度监控与盘口刷新分开，各市场常规采样目标间隔 2 秒。账户与多空循环同时启用且实际杠杆已就绪时，循环所选标的对应实际档位目标每 200 毫秒采样，暂停或关闭循环后恢复常规间隔；同标的跨账户共享，其他档位仍每 2 秒更新。接口按标的返回所有档位，200 毫秒轮次只提取当前循环所用档位。相邻请求按开始时间间隔调度，同标的不重叠，不补发积压轮次；公共提醒复用该采样，在账户唤醒信号投递后处理，不增加额度查询。普通账户至少等待 10 秒、暂停账户至少等待 60 秒，10x / 20x 优先通道按上述规则提前调度。下单仍要求账户快照不超过 8 秒、盘口与标记价格不超过 3 秒。200 毫秒 / 2 秒均为目标开始间隔，慢请求、接口预算与退避会增加实际延迟，不保证即时成交。公共风控档位在独立任务中每 60 秒热更新，冷启动及缓存超过 5 分钟时等待后台数据，高频采样不会同步补查档位。循环开仓使用该公共额度热数据检查目标金额倍数，减仓不检查公共额度；详见下方独立多空循环。账户的风控上限继续由既有账户后台任务更新；前端复用账户快照计算当前档位余量，不为展示增加认证请求。旧版独立 `monitor.py` 仍按原 5 秒周期运行。

公共报价由一条 `wss://fstream.asterdex.com` 连接订阅三个标的的 `bookTicker` 和 `markPrice@1s`，实盘和使用真实行情的模拟账户共享缓存。连接随交易服务启动和关闭；只有买卖盘口与标记价格各自通过 3 秒时效检查时才使用 WS，心跳、重复或乱序事件不会延长报价寿命。缺少数据、报价过期或断线时回退原有 REST 查询和一秒共享缓存；REST 同样失效时等待下一轮，不使用旧报价交易。重连会清空旧连接缓存，并采用退避，避免持续快速重连。公共额度、档位、账户风控与订单操作继续使用原有 REST 路径，演示模式继续使用模拟行情。WS 使用系统代理设置，支持 HTTP 和 SOCKS 代理。

公共交易深度另用一条组合 WS 连接订阅三个标的的 `@depth@100ms`。启动或需要重新同步时，先缓存该标的增量，再通过 `/fapi/v3/depth?limit=1000` 取得初始快照；只有更新范围与快照序号衔接、后续 `pu` 连续时才发布共享订单簿。同步正常时不再轮询 REST 深度。每个标的的 REST 重建最小间隔为 **30 秒**，同标的并发需求合并，每次仍按 **20 权重**计费，避免反复断线或序号缺口形成重建风暴；接口要求更长退避时遵守接口等待时间。连接、同步和缺口检查失败时停止使用该代快照，等待重新同步。

市场表显示 **1 万 / 5 万 USD1 深度价差**：分别沿卖盘买入、沿买盘卖出每边指定金额，按实际占用的各档数量计算成交均价，再计算 `(买入均价 - 卖出均价) / 两者中间价 × 10000`，单位为 bp，不含手续费。最后一档只计入足额成交需要的数量；任一侧不能足额成交时显示“深度不足”，不按部分成交外推。后台每 **1 秒**读取共享原始订单簿并在本地重算两档金额，汇总本身不发 REST 请求；每边最多使用 1,000 档，不能把未取得的更深档位视为可用。快照年龄超过 **15 秒**标记过期，读取或同步失败时保留上次结果并显示“更新失败”，首次失败直接显示原因；重新读取、心跳或重复事件不会给旧深度续期。深度任务与额度、BBO 和账户任务独立调度。普通新增开仓和右侧策略条件继续使用原有 BBO 与标记价格；迁移使用共享原始深度的 3 秒约束，两者都不把金额档的显示均价作为订单输入。

本批没有保留新增仓位时，日志显示各方向的订单终态及拒单代码；新开单按标的等待 15 秒，明确拒单从 30 秒开始，连续失败逐步延长至最多 120 秒。该等待会保留到重启后，成功加仓后清除；不会阻止升杠杆、已有订单核对和必要补偿。本地预算在发送前拦下的请求标记为“本地未发送”，不会当作未知成交反复查单，也不消耗实际补偿尝试次数。已经发送但响应不确定的订单仍严格查询核对，不重发。

两腿批量委托不是原子成交。如果两腿成交数量不同，程序先精确核对每侧实际持仓是否等于原持仓加本批净成交量，再检查该标的总持仓数量差。在 0.1% 内即可完成核对；超出时只平掉本批新增、位于当前较大一侧的多余数量，最多自动补偿 3 次，回到容差内即停止。原有持仓不会被补偿订单平掉。0.1% 容差不适用于回执与账本的核对：即使外部变化很小，也须暂停核查。超时或响应不确定时按已保存的 client order ID 查询，绝不重发原开仓订单；重启继续核对。补偿未完成或发现外部成交、ADL、持仓不符时暂停该账户，并显示原因。

确认批次和补偿前会重新检查账户快照时效。即使本批新增已全部平回，手续费与价差也可能降低权益，因此批次结束状态和后续风控检查标记一起写入数据库；重启后先补做检查。成交后权益不足或占用超限会持久暂停，暂停原因保留到人工确认后重新启动。

“暂停策略”停止新开仓和新杠杆调整，已提交批次仍继续核对和必要补偿。要停止所有请求使用 `sudo aster-desk stop`。网页的“重新核对”不会重发原开仓单；它允许重新检查与再次补偿。如果订单始终查不到或外部仓位变化无法与回执对应，记录会保留，必须先在交易所核查原因；本版不提供一键丢弃未确认订单的功能。

杠杆调整收到已识别的明确拒绝代码（例如仓位超限 `-2027`、杠杆无效 `-4028`、签名无效 `-1022`）时，结束本次调整并至少等待 30 秒重新评估，不会把已拒绝的请求留作未确认批次。HTTP `429` / `418`、请求限流代码 `-1003` / `-1015` 和没有明确未知执行代码的 WAF `403` 同样按拒绝处理，遵守原有退避与 `Retry-After`；WAF 默认至少等待 30 秒。退避结束后重新检查账户、额度及目标，风险占用达到上限本身不会禁止升杠杆；确认成功后才按新杠杆重新评估加仓。超时、网络中断及无法判断的服务端响应仍保留待核对记录，不自动重发。调整回执、最初错误代码和 HTTP 状态会持久保存；超时提示显示标的、原杠杆、目标及实际杠杆。旧版本留下的未知批次同样需重新核对，只有实际杠杆达到或超过目标才确认完成；该确认也可能来自人工调整，不证明最初请求成功，且不会自动重新启用已暂停的实盘账户。

恢复策略时，选择对应账户并点击“启动实盘”（模拟账户为“启动模拟”）。暂停账户的自动刷新较慢，页面上旧的快照或旧的异常状态不会阻止请求恢复；服务器会重新读取账户并核验实盘开关、固定模式、权益、交易权限和未完成订单，通过后才启用并刷新页面中的账户数据。未完成批次必须先核对，其他未通过的条件会显示具体原因。当前杠杆低于配置的最低开仓杠杆时，即使策略已启动，也会等待满足条件的升档并确认后再新增开仓。

市价成交可能偏离下单前的 BBO；标记价格和未实现盈亏变化会改变实际占用率，手续费、价差和滑点也会减少成交后权益。预测约束不保证任何时刻实际占用率都不超过适用上限；程序检测到超限时暂停加仓，不会自动平掉整个现有组合。升级前已保存的限价批次继续按原订单 ID 核对，不转换类型或重新发送；升级后新开仓和必要补偿使用市价单。

### 循环持仓不一致后的人工核对

实盘循环订单回执已确认、但持仓数量暂未同步时，先显示“等待持仓同步”，通过账户调度每隔至少 5 秒重新获取持仓；累计 3 次有效读取仍与回执推算数量不一致，才暂停账户并显示多空预期数量和实际数量。等待期间不重发原单、不提交补偿单、不开始持仓计时或下一轮；中途一致则继续原批次。次数与下次核对时间持久保存，重启不会重新计算已完成的次数。查询失败或过期不计入数量不一致次数，继续沿用接口退避；杠杆、账户模式和内部持仓记录异常仍按原规则处理。已暂停的账户不会因此自动启动。

出现“循环实际多空数量与记录不一致”时，异常文字下方显示 **已核对**，包括点击“启动账户”时发现的异常；启动失败的原因会持久保存，刷新页面后仍可处理。即使暂不满足恢复条件，入口也会保留，点击后显示未完成批次等具体原因。符合条件时读取交易所最新持仓，逐方向展示本轮原始数量、新增数量、记录应有数量、实际数量和差额。确认前会再次读取；账户设置、循环记录、实际数量或杠杆变化，或预览超过 5 分钟时，必须重新核对。

点击 **确认已核对** 会结束旧循环的持仓跟踪，将当前实际多空数量作为原始持仓保留，清除旧计时；仍持有的本轮新增仓位也会保留，不再按旧循环自动减回。操作不下单、不调整杠杆、不清空持仓，成交量与成本历史保留，旧状态和核对结果一并存档。账户保持暂停，之后点击 **启动账户**，通过现有检查后开始新一轮循环。如果有未完成订单，先使用 **核对未完成批次**；这个入口不会跳过未确认成交或补救订单。

## 飞书、升级与运维

新版 `aster-desk` 支持两种飞书通知，共用环境文件中的 `FEISHU_WEBHOOK_URL` 和可选的 `FEISHU_SIGN_SECRET`，无需运行旧监控服务。已在 `/etc/aster-desk/environment` 配置过机器人时，升级后直接沿用，不需要再填一次。两种通知共用持久化发送队列，交易汇总优先；飞书已收到但响应丢失，或发送后进程中断时，重试仍可能重复投递。

**公共额度提醒**监控 XAUUSD1、SPCXUSD1、CLUSD1 的 5x / 10x / 20x，以网页“普通开仓”中保存的额度阈值为准。单账户设为 100,000 USD1 时，额度必须严格大于 100,000 才提醒，等于阈值也不触发。多账户时，按关注该市场的账户中最低阈值触发，每个市场和档位仍只发一条消息，列出满足额度阈值的账户及其设置；其他市场的账户阈值不参与。暂停或尚未启动的账户仍参与提醒，没有账户关注的市场不发送。它复用现有行情快照，不增加 Aster 额度查询。提醒仅判断公开额度，不代表已满足最低开仓杠杆、价差、个人持仓容量和风险等全部交易条件。

配置了 Webhook 后，公共额度提醒默认启用。可在同一环境文件添加以下设置，保存后执行 `sudo aster-desk restart`：

```dotenv
ASTER_CAPACITY_ALERT_ENABLED=1
ASTER_CAPACITY_ALERT_COOLDOWN_SECONDS=300
```

`ASTER_CAPACITY_ALERT_ENABLED=0` 只关闭公共额度提醒，交易汇总继续发送。冷却时间单位为秒，默认 `300`。旧环境变量 `ASTER_CAPACITY_ALERT_THRESHOLD` 不再覆盖网页设置，可以删除。额度阈值在网页保存后，无需重启服务，下一次有效行情按新值判断；发送前再次核对配置，旧阈值产生的待发送或失败重试消息不会沿用旧门槛发送。没有任何账户时不回退到默认 10,000 提醒。

每个组合首次取得高于阈值的有效额度时提醒一次，持续高位不重复提醒。额度回落至阈值及以下后重新升高，并且距上次成功提醒至少经过冷却时间，才再次提醒。成功状态保存在交易数据库，后续重启继续去重。失败通知持久化重试，发送时使用新的有效额度；数据过期或缺失时等待，额度已回落时不发送。未配置 Webhook 时不积累公共额度提醒历史，配置后依据新的行情快照判断；演示模式不发送。

**交易完成汇总**在实盘完成一轮连续加仓后，因暂停、达到风险限制，或连续 60 秒不能继续加仓而结束本轮时发送，包含账户、成交批次、每个标的与杠杆、总名义金额及实际保证金占用率。交易汇总失败会重试；没有配置 Webhook 时仍保留待发交易汇总。网页显示待发送数量。

```bash
sudo aster-desk status
sudo aster-desk logs
sudo aster-desk stop
sudo aster-desk start
sudo aster-desk restart
```

重复安装命令即可升级。先构建新版本，再切换 `/opt/aster-desk/current`；健康检查失败恢复旧服务。账户、策略开关、未完成批次、通知和新版公共额度提醒状态保存在 `/var/lib/aster-desk/trading.sqlite3`，环境文件和数据库不会被升级覆盖。重启后已启用策略会继续运行；升级前可在网页暂停账户。

循环 WS 唤醒和成交质量记录随本次一键升级启用，不需要新增服务、环境变量或账户配置。最新观测与历史循环批次一同保留在原交易数据库中；没有历史盘口或本地计时的旧批次显示未知，升级不会补造这些数据。

循环账户读取并行化、一次原子落盘与分项耗时也通过原一键升级启用，无新增依赖、数据库迁移或配置步骤。已有持仓与暂停状态仍从原数据库恢复。

升级会显示各阶段和总耗时。系统依赖齐全时跳过系统包更新，并复用已下载的 Node。Python 锁文件和运行时未变时复用虚拟环境；前端源码未变时直接复用构建结果；仅前端源码变化时复用 npm 依赖并重新检查、构建。首次使用新版安装脚本仍需建立缓存，后续升级才能完整提速；依赖或运行时变化会重新准备对应部分，下载和服务切换仍取决于服务器网络和运行状态。

缓存位于 `/opt/aster-desk/cache`，按依赖、源码和运行时指纹区分。当前版本的 Python 环境可能链接到其中，服务运行时不要清空整个缓存目录。构建不会改动正在运行的 Python 环境，并发安装会被拒绝；部署新版本仍执行导入检查和健康检查，完全未变时只检查现有服务健康。

安装器在升级前和成功后回收旧版本，通常保留当前版本和一份成功回退版本，同时保护服务实际运行的目录、安装来源、配置的数据目录以及它们引用的 Python 环境。首次迁移旧安装器时，缺少成功标记的旧目录只保守保留一份结构完整的候选，不将它当作已验证的回退版本。未知目录、外部链接和含挂载点的目录不自动删除；因此这些受保护项目可能使保留数量超过两个。

不再使用的 Python、npm 和前端构建缓存，每类最多保留一份，合计最多 1 GiB；当前及回退版本引用的 Python 环境属于运行必需文件，不计入此缓存上限。空间紧张时优先回收这些可重建缓存。构建前检查磁盘容量和 inode，容量仍不足就退出并保持原服务运行；失败构建在回滚后回收，原配置和 SQLite 保留。相同源码、依赖、运行时和服务配置且服务健康时，不新建 release、不重复构建或重启；远端提交相同也不重复下载源码包。

仅清理已有部署、不升级或重启，可以在服务器执行：

```bash
curl -fsSL https://raw.githubusercontent.com/hxx344/aster_5x/main/install-trading.sh | sudo bash -s -- --cleanup
```

该命令仅处理安装器能识别的旧版本和缓存，不清空数据库、历史记录或整个系统缓存；实际释放量取决于服务器遗留文件。无正常服务目录信息、数据目录无法确认或路径不符合约定时，停止清理并报告原因。

新交易服务健康后，安装脚本停用旧 `aster-5x` 独立监控服务，避免重复轮询。新版直接支持公共额度提醒和交易汇总，不启动旧版 `monitor.py`；旧配置和历史文件仍保留，但不会自动导入旧 `alerts.json` 的提醒状态。从旧独立监控迁入时，若新环境文件尚无机器人配置，需填写 Webhook 和可选签名密钥，并在网页保存账户额度阈值。首次启用或变更网页阈值后，按新设置已达标的组合可能各提醒一次；此后按新数据库中的成功状态和冷却规则去重。

备份时先停止服务，再备份 `/var/lib/aster-desk` 和 `/etc/aster-desk`，完成后启动服务。一个数据库只允许一个服务进程。不同数据库或服务器不会共享进程锁，不要把同一真实账户同时交给多个实例。

登录会话最长 12 小时，退出或重新登录会撤销旧会话；服务重启需要重新登录。请求体按实际接收字节限制为 16 KB，并限制接收时间；登录失败记录和会话数量均有内存上限。健康接口在调度线程退出或服务停止后返回 HTTP 503。行情、账户和通知有独立的并发容量，账户列表最多每秒读取一次；网络请求仍受共享预算与交易所退避限制。

## 开发验证

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.lock
.venv/bin/python -m unittest discover -v
.venv/bin/python -m tests.benchmark_risk
cd dashboard
npm ci
npm run typecheck
npm run lint
npm run build
cd ..
.venv/bin/python -m trading.server --demo
```

演示环境使用明确标记的模拟行情与独立账本，CLI 限制回环地址，拒绝添加实盘账户。CLI 和 `create_app(demo=True)` 均默认使用 `runtime/demo`；正式入口默认使用 `runtime/trading`。HTTP 应用建立前以数据库用途标记隔离两种模式，显式设置 `ASTER_TRADING_RUNTIME` 或传入 Engine 也不能跨模式复用。正式入口可接纳已有未标记的正式账本；演示入口只自动认领空库，已有来源不明的库会被保留并拒绝匿名打开。升级后遇到旧演示库无标记时，可用 `ASTER_TRADING_RUNTIME="$PWD/runtime/demo-v2" .venv/bin/python -m trading.server --demo` 启动新的演示目录，不删除旧数据。

前端静态文件由 Python 服务直接提供，不需要在生产环境运行 Node。认证账户接口、真实成交和飞书发送必须在配置服务器凭据后核实；自动测试使用模拟接口，不代表实盘已验收。

回归、故障注入、随机场景、依赖审计和本地性能结果见 `VALIDATION.md`，各项记录注明对应验证日期和范围。

接口依据：[Aster V3 签名规则](https://asterdex.github.io/aster-api-website/futures-v3/general-info/)、[账户与交易接口](https://asterdex.github.io/aster-api-website/futures-v3/account%26trades/)、[市场数据](https://asterdex.github.io/aster-api-website/futures-v3/market-data/)。服务器需启用时间同步，以满足 V3 nonce 时效要求。
## 独立多空循环

在每个账户的“多空循环”面板保存参数，再启动该账户。功能默认关闭；保存配置不调整杠杆、不发送订单。循环与普通额度加仓可在同一账户按品种并行，与 XAU 仓位迁移仍互斥，其他账户各自运行。

本账户一旦选择某品种的成交量循环，普通加仓与升档入口就禁止对该品种提交 5x、10x、20x 新增仓位或普通杠杆调整，额度高于阈值也不会放行。检查同时读取本次调用与建单前保存的账户配置，旧账户对象不能绕过已启用的循环设置。额度优先唤醒和普通候选筛选只排除本账户的循环品种，其他普通品种及其他账户的同品种继续执行。

页面的所选账户市场表保留交易所公共额度，循环品种标明普通加仓已禁用，不再用绿色额度触发色表示该账户可普通加仓。账户暂停、UTC 日额度耗尽期间仍保留保护；完成仓位与批次处理后关闭循环模式即可解除。正常循环开仓、平仓、已有订单核对及必要减仓补救不经过这个普通新增开仓拦截。

“普通开仓”页的条件面板展示各档额度、当前占用率对应的普通档位上限和 BBO 价差。额度、账户快照和盘口分别依据自身采样时间判断是否过期，刷新页面不延长旧数据的有效期；各数值条件满足不代表已通过下单前的全部检查或解除循环品种保护。顶部“启动账户 / 暂停账户”是唯一的账户总开关，按显示的已保存执行范围启停；切换功能页只切换视图。

同一账户先核对未完成批次和成交后风险，再推进循环与其他品种的普通检查。循环等待价差、持仓计时或成交量窗口不阻断普通品种；共享接口预算、交易所退避、账户模式异常与循环仓位归属异常仍按账户处理。每次交易后重新读取余额和全部持仓，账户总保证金上限不会按策略重复分配。调度预算计入两项功能的查询成本；循环结束后不修改其他品种的普通仓位。

| 参数 | 含义与示例 |
| --- | --- |
| 实际杠杆 | 仅使用所选品种当前实际杠杆，空仓同样沿用交易所当前设置；循环不自动升降杠杆。当前档位额度不足、缺失或过期时等待，即使较低杠杆额度充足也不回退、不借用其额度。旧配置的 `leverage` 字段仅保留兼容，不能授权修改杠杆。 |
| 品种 | XAUUSD1、SPCXUSD1 或 CLUSD1，可任选；每账户一次只能运行一个，本轮结束并暂停后才能切换。 |
| 价差采样金额 | 多空两边各以此 USD1 金额扫盘口，示例每边 10000。 |
| 价差阈值 | `(买入加权均价 − 卖出加权均价) ÷ 两者中间价 × 10000`，单位 bp。0.1 bp = 0.001%；不包含手续费。开、平仓使用同一阈值，均为小于等于。 |
| 名义金额范围 | 选择“每边”或“多空合计”，示例每边 0–10000 USD1。每轮新增多空数量相同，原始多空数量可不相等，按步长向下取整，在深度、余额、循环有效保证金上限和交易规则内取最大可执行数量；0 下限仍须满足交易所最低委托。 |
| 开仓额度倍数 | `capacity_multiplier`，默认 1，范围 1–100，支持小数。公共剩余额度须 ≥ 多空合计目标金额 × 倍数；目标取名义价值上限，按每边配置时乘 2。例如每边 10000、2 倍，额度阈值为 40000 USD1。 |
| 持仓时长 | 1 秒至 7 天，示例 60 秒。多空完整成交并经实际仓位确认后才开始计时。 |
| 每日成交量上限 | 每账户独立设置，单位 USD1，范围 0–1000000000000；默认 0 为不限。各品种分别使用此上限，各自仅约束 UTC 自然日成交量；切换品种不清零。 |

循环顺序为记录原始多空数量及实际杠杆 → 公共额度达到目标金额的设定倍数 → 热差价满足 → 同批提交多空加仓 → 核对成交 → 持仓计时 → 时间到达后等待减仓价差 → 同批减回本轮新增 → 核实恢复原始数量及杠杆 → 下一轮。额度只用对应实际杠杆、从请求开始起不超过 1 秒的有效样本，未知或过期时等待；账户档位余量、余额、保证金及成交量窗口仍需通过。最终计算和订单落盘后、发单入口前再次检查公共额度，下降或过期时确定不发单，也不补发这批旧开仓单。参考金额的深度价差与实际委托数量的深度价差必须同时达标；深度不足、超过 3 秒或 WS 连接/序号失效时等待有效数据。减仓和补救不检查公共额度，包括额度为零、查询失败和过期的情况；只减少本轮新增数量，原始多空数量保留。账户请求遵守共享预算和退避，因此持仓时间是最短等待时间，不保证恰好到秒成交。

有效的 BBO 或已同步深度 WS 更新会唤醒本地调度，接收回调本身不查询账户或下单。开仓预筛先检查公共额度倍数，再读取共享完整深度；减仓跳过公共额度。按精确值检查参考金额的价差、开仓最低可行数量或本轮全部平仓数量；过期、深度不足、持仓时间未到或公共条件不满足时不派发优先任务。它返回的数量只是候选依据，账户任务随后读取最新配置、仓位和风险，重新规划实际数量，并在提交前按同一实际数量复核最新完整深度。

同账户、同等待阶段与配置的重复达标机会合并，优先派发至少间隔 **1 秒**。单账户仍只运行一个交易执行任务；执行任务忙时只保留最新机会，恢复空闲后重新检查时效和条件。仅明确的公共额度、价差或深度条件等待允许被新机会提前唤醒，余额、保证金、成交量、接口预算及未知异常的退避不被行情更新解除。循环优先任务不推迟普通任务原定的到期时间，也不绕过暂停、未决批次核对或成交后风控。没有有效 WS 信号时继续定时检查本地条件；行情恢复由后台处理，原有请求预算继续生效。

循环正常开仓和平仓从 **行情触发到首次下单之间为 0 个 HTTP 查询**。启用的实盘循环有独立后台账户任务，目标每 2 秒读取 `accountWithJoinMargin`，按需更新保证金模式与补充字段；历史成交补账也有独立任务，不占交易执行槽。交易线程只读取账户热快照、WS 报价和已同步完整深度，缺失时等待后台更新，不现场补查或重建盘口。最终仍按实际委托数量复核最新完整深度及全部风控。

网页每 3 秒读取账户状态，运行中的实盘循环直接展示后台已核验且更新的账户快照，不必等待至少 10 秒的普通策略轮次。仓位、余额、权益和占用率使用同一份快照；状态接口不额外查询交易所。后台断开、过期或数据被订单事件撤销时，保留最后显示的数据和原始读取时间，不退回更早的读数，也不刷新旧数据的时间。页面展示副本不改变交易执行使用的视图或快照资格；网络耗时和请求预算退避仍会影响实际更新间隔。

私有账户 WS 使用现有 V3 签名客户端在后台创建 `listenKey`、每 30 分钟续期，并在连接期限前重连。账户、订单、配置及未知事件均撤销旧快照资格，配置事件还强制刷新模式；断线或异常立即撤销资格，重连后必须重新取得完整基线。推送只报告变化，不能从缺失字段推断零余额或零仓位；后台读取若跨越账户事件、写入或断线，不得发布。新快照发布也会撤销已取得的旧资格，最终风控后及原子订单落盘后、发单前都复核。发生本地失效时按确定未发送处理，保留原订单身份与恢复规则。

账户 `positions` 的 `LONG` / `SHORT` 行直接确认双向模式，不再额外查询 `positionSide/dual`；出现 `BOTH`、缺行、重复或异常字段均拒绝本轮。价格使用 3 秒内的本地标记行情；行情覆盖所有非零 USD1 持仓时无需 `positionRisk`，缺少行情时才在后台补查并核对全账户数量、杠杆和保证金模式。账户响应没有强平价，未补查时显示未知。全账户仓位占用和保守浮盈亏计算继续保留。

循环采用账户两边 `maxNotional` 的较小值作为当前实际杠杆总上限，先扣除所选品种原有多空的合计名义额，再计算本轮新增量；不能把原仓占用当作剩余额度，也不能用其他杠杆档位授权。字段缺失才在后台补查 `leverageBracket`，并保留原始请求起点的 5 秒有效期；非法值直接拒绝。已有持仓时若开仓额度不可用，不发布可用于新增的额度；本轮减仓及补救仍可核对并执行。`multiAssetsMargin` 在首次连接、重连及模式事件后强制更新，平时最多缓存 15 秒；热快照不能超过模式缓存的原始期限。普通策略、启动准备与杠杆调整核验继续走各自流程。

每笔后台请求保留原权重、预算权限、退避和唯一签名 nonce；常规账户读取权重为 5，需要更新模式时另计 30，缺字段补查仍按实际接口计费。热交易路径仅预留批量下单的 5 权重。后台目标频率受共享预算和退避约束，多账户或接口限流时可能延后；过期就等待，不能占用恢复保留额度强行更新。快照最长有效期从后台读取起点算起为 8 秒，档位、模式和盘口各自期限同时生效，读取、等待或本地复核均不延长旧数据寿命。热读取按当前本地标记价重算全账户占用和保守浮盈亏。

新循环批次、成交量索引和原执行事件在同一 SQLite 事务内以 `synchronous=FULL` 提交，成功后才调用下单；任一步失败整体回滚，不覆盖已有批次 ID。发单前估计直接复用最终风控计划的实际委托数量与双边金额计算均价，不重复扫描深度；参考金额的价差不能替代实际批次价差，平仓时仍正确映射 BUY/SHORT、SELL/LONG。

等待开仓或平仓时，“多空循环”页保留简短状态，详细检查放在“记录 → 执行记录”中默认折叠，展开后查看最近检查的 UTC 时间、当前值、要求值及单位。价差超限会列出实际 bp、阈值、采样金额和买卖均价；完整深度不足则列出双边可用深度，不外推未取得的均价。不能满足最小一轮时，以满足交易所数量、步长、标记价最低名义额及配置最小金额的最低数量为依据，逐项展示金额上限、档位容量、可用余额、预计账户权益、保证金占比及成交量余量，能算出时同时显示资金缺口。该数量是解释拒绝原因的检查依据，不是下一笔承诺下单数量。

“记录 → 成交质量”展示“最近循环成交质量”概览，其盘口、成交与响应明细默认折叠；“多空循环”页继续只保留简短状态。它以本批每边实际委托数量为共同口径，展示三阶段的买入、卖出加权均价和 bp 价差：

| 阶段 | 数据依据与确认条件 |
| --- | --- |
| 触发快照 | 在产生候选机会时读到的有效完整深度上，按本批数量分别扫卖盘和买盘。展示盘口采样时间与检查时间；缺少快照或无法覆盖整批数量时显示未知。 |
| 发单前估计 | 使用最后一次提交前复核通过的同一份完整深度，按同一批数量计算，保留其盘口时间与检查时间。 |
| 原始双边成交 | 仅当原始 BUY、SELL 两条订单均确认 `FILLED` 且各自成交数量等于本批数量时，使用回执实际均价计算。平仓的 BUY 属于 SHORT、SELL 属于 LONG，仍按买入减卖出的同一公式展示。 |

三阶段价差均为 `(买入均价 − 卖出均价) ÷ 两者中间价 × 10000`，单位 bp，不含手续费。部分成交、待确认、拒绝、本地未发送与未知结果保留各自状态，未确认完整双边成交时不展示完整成交价差。修复成交不并入原始双边价差；即便原始双边已完整成交，后来发生修复时也明确标识，因此该记录不代表含修复的整轮总成本。价格、数量和价差以十进制字符串保存，展示计算使用独立的 36 位有效数字精度，不参与下单决策。

耗时分别为本地 WS 接收至调用 `broker.submit`、最终深度复核完成至该调用、该调用开始至返回响应，单位毫秒；阶段时间以 UTC 显示。计时使用同进程单调时钟，请求开始是本地调用边界，不是交易所接收或撮合时间。调用抛出超时或其他异常时，响应时间保持未知，即使之后查询确认成交也不会补造该耗时。重启保留原已记录值，无法恢复的盘口或计时显示未知。观测只复用本地深度和已有订单回执，不增加行情或账户请求，不为每条 WS 更新新增日志；它用于核对实际执行质量，不保证实盘成交价差变好。

“发单准备耗时”将 WS 接收至请求开始拆成七个不重叠部分：触发至取得账户任务锁、账户热数据读取、首轮规划与盘口、提交前账户复核、最终盘口与风控检查、订单记录写入、其他本地准备。第一项包含本地预筛和等待，不仅是排队；热数据路径的“提交前账户复核”是本地快照时效与条件检查。行情预备和最终规划的耗时相加但不重复计入账户查询；“其他”仅在总耗时与前六项均有效且能一致相减时计算。最终深度检查至请求开始是总耗时内的子区间，不能再次相加。普通定时检查没有 WS 接收时间时，相关未知项保持未知；旧记录、观测失败或落盘后立即崩溃也不补造耗时。所有分项仅用于观测，不改变交易授权。

检查项目分别标为已满足、未满足或未核验；未列出的条件不代表通过。深度不足时无法可靠计算的费用、金额或保证金项目显示未知，非有限小数的展示近似值带 `≈`，交易判断仍使用原有精确数值。诊断中的“风控手续费预留率”来自账户快照，与下文固定 `0.0125%` 的事后成本统计口径分别使用。页面轮询不改变实际检查时间；当前状态诊断会随下一次检查、成功进入下一阶段、暂停或其他错误更新或清除，执行记录中的历史检查仍保留。

后台只将没有未完成批次的循环条件等待记为合并检查。同账户、同品种、同开/平仓阶段的连续检查累计次数，保留首次时间并更新最近时间、最新原因及具体数值；价差、最低委托或成交量余量等等待原因交替变化也不再刷出多条。同账户出现成交、下单、修复、操作异常、启停或配置事件时断开合并，其他账户不影响计数。未决批次核对、账户模式/仓位归属异常、接口和预算错误按原记录处理。数据库升级自动增加可空的检查元数据字段和时间排序索引，不删除历史记录；重启继续累计同一段检查。旧版历史仅在页面按明确的条件标题合并当前加载到的条目，标明品种未记录，不推断其属于当前选择。

提交前成交量复核额外给出 UTC 当日已用量、剩余量、本轮预计开平量与上限；成交后回退原因给出实际多空名义额及允许范围，或总保证金、权益、实际占比及有效上限。详情使用现有检查输入，不新增交易所查询，不改变采样周期、交易门槛或必要补救流程。

每日成交量按账户和品种分别累计，只统计该品种循环订单每笔实际成交的名义金额之和，包括多空两边的开仓、平仓和补救，不含手续费，也不混入普通加仓、迁移或手动订单。例如每边开仓 10000 USD1，一轮开平仓约累计 40000 USD1。每个品种各自使用所设上限，仅约束 UTC 自然日成交量。下单按当日剩余额度为本轮开仓及预计平仓预留额度，提交前再次核对；剩余额度不足时按数量步长缩小，若连交易所或配置的最小一轮也无法满足，则等待日额度。UTC 00:00 重置日额度，昨日成交不再占用今日额度。账户保持启动，日额度充足且价差、深度和风控条件满足后自动恢复，无需等待成交满 24 小时。手动暂停保留暂停状态。平仓和必要补救不受上限阻挡，因此实际成交价格变化或补救可能使最终累计量超过设定上限。

网页显示今日累计及剩余额度，保留近 24 小时成交量、成本与统计时间；近 24 小时仅作统计，不限制开仓。滚动统计窗口为 `当前时间 − 86400秒 < 实际成交时间 ≤ 当前时间`，成交恰好满 24 小时后移出统计。迟到成交和重启恢复使用同一份持久逐笔账本；当前 UTC 日内完成或跨日未补齐的订单仍需核对，昨日已完成订单的待补账仅影响历史统计。

当前所选品种的循环成本以 USD1 展示，分为手续费、成交差价和两者合计，同时汇总 UTC 当日与滚动 24 小时，并显示在逐笔成交明细中。手续费按用户指定的固定 taker 费率 `0.0125% = 0.000125` 乘以每笔实际成交名义金额计算，开仓、平仓和补救成交均计费。例如一轮成交量 40000 USD1，对应此口径手续费 5 USD1。它是成交后的统计口径，未改动订单风控的手续费预留或模拟账户的扣费设置；未计入资金费、返佣及其他账户流水。

成交差价按同账户、同品种、同批次的 BUY 与 SELL 实际数量先成交先配对，计为 `(买入成交价 − 卖出成交价) × 配对数量`；多空开仓和平仓各计一次，补救与所属原批次一并配对。有利成交差价允许为负并抵减手续费，不对未配齐的单腿按全部买卖金额计算成本。差价只记入完成配对的较晚成交一次：跨 UTC 日时，手续费各归实际成交日，配对差价归较晚成交日；滚动窗口使用同一归属时间，窗口外成交仍可作为配对依据。

未配齐数量显示待配对金额；订单或明细仍在核对时，汇总标记成本未完整，已知数值只代表已入账部分。成本读取或计算失败时显示未知，不显示为零。历史已入账成交沿用上述费率自动补算，迟到成交、重启和重复读取不会重复加收；旧模拟成交的估算时间继续保留标识。成本统计只读取本地循环账本，不新增交易所请求或改变交易量额度。

成交明细按实际 UTC 成交日期入账，跨午夜的两笔成交分属各自日期。交易所成交 ID 去重后持久保存，重复核对、服务重启或改参数不会重复累计或清空历史。实盘由后台使用 `GET /fapi/v3/userTrades`（每次权重 5，最多 1000 条）匹配本循环订单，分页进度可恢复，循环执行流程不查询逐笔明细。每日额度不限时，补账不阻塞下一轮；有日上限时，从已核实的终态回执 `cumQuote` 累计成交金额中减去已入账部分，将余下金额预留到当日额度，明细入账后自动替换，剩余额度包含该预留。该字段见 [Aster V3 订单接口文档](https://github.com/asterdex/api-docs/blob/master/V3%28Recommended%29/EN/aster-finance-futures-api-v3.md#query-order-user_data)。跨日尚未分清的部分保守预留到当日，实际成交统计仍只按明细归属日期；旧回执缺少金额、回执与明细矛盾或当日待补账超过 1000 批时，有日上限的下一轮等待后台核对。订单结果或持仓尚未确认仍须核对完成，已有仓位仍能平仓或补救。网页列出最近 100 笔成交及每笔所在品种及日期的累计量，晚到记录按实际成交顺序补入并校正累计。历史模拟订单若缺少原始成交时间，会明确标注为估算时间；新的模拟成交使用持久化的实际模拟成交时间。

每轮只处理叠加在原有仓位上的新增数量。原始多空数量、杠杆与本轮新增量分别持久保存，正常减仓及部分成交补救最多减到原始数量；仓位变化与回执不符或本轮杠杆改变时暂停核对，不靠补开原仓掩盖差异。这里保持的是原有多空数量和杠杆；交易所会按真实成交调整持仓均价、盈亏及手续费。循环所有杠杆统一使用 `min(账户基础风险上限 + 5 个百分点, 100%)` 作为账户总保证金占用上限，循环自身不另设权益 5% 的限额。例如基础 90% 时有效上限 95%，基础 98% 时封顶 100%。其他品种、多空方向和已有持仓共同计入这一个总上限，普通高杠杆、迁移和循环不会在同一账户上重复叠加多个 5 个百分点。账户保存的基础设置保持原值。

开仓前按包含手续费与盘口成本的预计成交后权益检查总占用，并同时检查可用余额、深度、交易规则及两个成交量窗口；空间不足时缩小本轮或等待。开仓金额上限按提交前盘口估值，市价成交期间价格仍可能变化；成交核实使用同一有效保证金上限，发现金额越界或实际占用超限时回退本轮新增仓位。重启后未完成开仓批次沿用同一复核规则，平仓和必要补救不受新增开仓的保证金上限阻挡。

“同时”表示通过同一批量请求提交两边，交易所不会保证两边在同一瞬间成交。开仓只部分成交时，回退本轮已经成交的仓位；平仓只部分成交时，只继续减少本轮剩余仓位。此类补偿优先恢复仓位一致性，不再等待持仓时长或价差阈值。补偿次数有限，未完成或结果不明时保留批次并暂停核对；结果不明的订单不会重复提交。已回退的失败开仓先冷却再重试。

暂停保留已有仓位及持仓计时，不主动平仓；恢复后沿用记录继续本轮。循环仍有本轮新增量或待核对订单时，不能修改参数或退出循环模式。本轮新增全部减回后暂停账户即可修改，原有仓位可以保留。循环记录、成交确认和下一阶段在同一数据库事务保存，服务重启后先核对未完成批次，核实恢复原始数量前不会重复开启下一轮。

旧账本升级时在同一事务中按账户、品种和 UTC 日期建立统计，并重算历史逐笔累计；交易 ID、历史成交及总量保留，重复升级不会重复入账。旧版从空仓开始且尚未结束的循环沿用零原仓记录完成减仓，未完成的旧杠杆请求只核对、不重发。

循环复用现有鉴权、账户锁、模拟/实盘隔离与 `ASTER_ALLOW_LIVE` 开关。所有交易使用 taker 市价单，循环前台与后台均不请求 `openOrders`；快照以 null 表示未查询。自己的未决订单仍按持久化批次及回执核对，未知结果不重复发单。升级继续使用文首一键安装命令，无需新增配置或依赖，保留原配置与账本。

如需回退至没有循环功能的旧版本，应先暂停循环账户并完成持仓、批次处理；旧版本无法识别循环记录，保留启用状态直接回退可能恢复旧策略。
