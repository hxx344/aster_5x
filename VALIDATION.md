# 交易服务验证记录

## 暂停账户快照刷新与显示状态（2026-09-21）

复现了保存循环配置后暂停账户不再读取快照的问题：循环专用分支提前返回，普通快照读取被跳过，而循环后台任务在暂停时也停止刷新。修复后，无未完成批次的暂停账户继续走原有预算调度读取余额与持仓，随后更新暂停展示；循环配置、已有仓位和持仓计时保留，不启动策略、不下单、不调杠杆。已有批次仍优先走原核对路径。

状态接口提供账户当前目标刷新间隔，仅供展示使用。顶部、持仓和模式说明区将超过 8 秒的数据显示为最近快照；在正常定时间隔内显示“定时更新”，超过目标间隔加 8 秒（最长等待展示窗口为 120 秒）时显示“更新延迟”。账户错误和页面断线单独警示。旧服务缺少间隔字段时保持 8 秒警示；读取展示不会请求交易所，也不延长旧快照的交易有效期。

Windows 原生验证：新增 5 项后端测试，连同账户调度、循环执行、恢复启动、后台热数据、快照竞态和删除约束，共 109 项通过。新测试在修复前复现循环单独运行配置和混合配置暂停后快照调用次数为零；修复后还检查失败保留原时间、后续恢复、暂停状态和持仓计时保留。实盘适配层使用模拟接口验证仅发送 GET。前端新增 6 项显示测试，与原模式测试合计 16 项通过；类型检查与 lint 通过。

Playwright 在隔离模拟页面验证正常等待、13 分钟旧快照、账户读取错误及页面断线；1440 像素桌面和 390 像素手机显示检查通过，没有页面横向溢出。截图保存在本地 `output/playwright/snapshot-refresh-*.png`。本次没有使用 WSL，没有运行 Linux 部署验收，也未连接真实交易账户或升级用户服务器。

## 实盘循环持仓延迟同步（2026-09-17）

实盘订单终态已知而持仓数量不一致时，每隔至少 5 秒重新读取持仓，累计 3 次有效读取仍不一致才暂停；等待期间不提交原单或补偿单。核对次数及下次读取时间随批次持久保存，一致后清除；异常提示补充多空预期与实际数量。模拟账本仍立即报告不一致，内部记录和杠杆异常不进入同步等待。

新增 16 项测试，连同循环执行、原始仓位保护、账户调度、成交补账和快照竞态回归，共 **87 项通过**。覆盖开仓/平仓/补偿延迟、频繁唤醒、服务重启、查询失败、过期数据、时钟回拨、原始仓位保留，以及暂停账户不自动启用。用提交前代码运行延迟开仓用例，确认旧逻辑会立即暂停。全部使用模拟账本和接口，未访问实盘账户或升级服务器。

验证日期：2026-09-14。范围为账户交易服务、公共额度监控、Web API、持久化和本地并发；所有交易接口使用模拟响应或模拟账本，未向交易所下单。

## 前端功能拆分与信息精简（2026-09-16）

工作台默认进入多空循环，普通开仓、迁移、账户与记录独立展示。账户启停集中到顶部，显示已保存配置实际涉及的品种；循环品种不重复列入普通开仓范围。基础风险配置移到账户页，四类配置各自提交所需字段。状态读取及会话处理集中于一个共享入口，各功能分别持有按账户隔离的草稿。长说明、精确数值、成本及诊断按需展开，保留过期、断线、异常和仓位核对入口。

- 前端 123 项测试通过，包含新增的账户执行范围回归检查；类型检查、静态检查及 Node 22 生产构建通过。
- Playwright 使用本地临时模拟账本及两个模拟账户，验证功能切换、刷新与账户切换保留各自草稿；普通、循环、迁移与基础风险保存请求均仅包含相应字段，保存保持暂停。
- 验证账户启动 / 暂停、运行中参数锁定，以及仍有循环新增仓位时所有配置页提前禁用编辑。原界面直到保存才返回持仓限制，本次已使前端提示与后端约束一致；停止迁移仍保留独立入口。
- 模拟状态接口 503 与 401，确认断线保留最近数据并标记过期、禁止启动，登录失效清除账户数据和未保存草稿；恢复连接后重新读取服务端配置。
- 检查 360、390、844、1440 像素宽度，四个功能页及展开表单、记录页的循环成交 / 执行记录 / 成交质量分类均无页面横向溢出；宽表保留局部横向滚动。桌面、手机页面截图保存在本地 `output/playwright/ui-simplification/`。

Codex Security 对固定功能补丁 `ca8ceb1..d38dc1b` 的 18 个变更生产源文件，以及直接关联的会话、配置和启停边界完成复核，未发现可报告问题。扫描 `331269aa-e659-4223-89fa-0a3fa8d0f91a` 已封存并回读：coverage 为 complete，findings 与 deferred 均为零。报告保存在本地 `output/playwright/ui-simplification/security/report.md`。检查没有覆盖真实部署配置或交易所执行；插件 Daybreak 访问状态为 unknown，本地补丁复核不依赖该受保护报告。

本次未修改交易服务、账本或交易所请求实现；已有后端回归结果见下节。全部浏览器写入与成交仅发生在临时模拟环境。

## 循环调度、账本与报告延迟优化（2026-09-16）

功能提交 `9076871`。循环信号复查最小间隔从 1 秒降至 100 毫秒，真正尝试提交后仍保留 1 秒间隔；确定未发送的拒绝不占用发单间隔。同账户的普通只读快照移到后台，读取期间释放执行线程与账户执行锁；普通与循环快照分别串行读取，网络等待不再占用共享缓存锁。结果必须仍属于同一账户配置、品种和 broker，并通过数据代次与时效校验；写操作、私有事件及缓存撤销后的旧结果不可发布。未结束的后台读取继续占用自身任务名额，避免取消失败后不断积压。下单、恢复和杠杆修改仍按账户串行；额度限制、最终发单校验和原有退避保留。

单次执行复用线程内数据库连接，但每次账本操作仍独立提交或回滚；不跨网络等待持有数据库锁或写事务，保留 SQLite `synchronous=FULL`。静默额度观察按品种批量提交，去重、冷却期、在途通知及事务回滚规则不变。迟到成交只重算受影响的累计后缀，修正版本触发器的同账户双重递增；事件和成交查询增加组合索引。成本仍校验和配对全部成交，只构造当前所需的 100 条展示明细；页面复用数字格式器。

Windows Python 3.11.9 同机对比基线 `56d2b7e` 与功能提交，使用隔离合成账本；所有对比的汇总、逐行累计值和可见明细完全一致：

| 场景与口径 | 优化前 | 优化后 |
| --- | ---: | ---: |
| 账户事件查询，10 万事件、较新 5 万条属于其他账户，20 次中位数 | 2.0503 ms | 0.1345 ms |
| 完整事件读取方法，同一数据集 | 5.7596 ms | 2.0469 ms |
| 9 组静默额度观察，20 轮中位数；提交数为总计 | 203.3418 ms、180 次提交 | 65.4041 ms、60 次提交 |
| 2 万成交后追加 1 条并插入靠近末尾的迟到成交，单次 | 435.6260 ms、20002 次累计更新 | 78.6529 ms、3 次累计更新 |
| 2 万成交成本计算，5 次中位数 | 567.7927 ms、20000 条展示明细 | 395.1476 ms、100 条展示明细 |
| 已准备账户从执行器进入至 broker 提交边界前，7 次中位数 | 41.8890 ms、12 次新连接 | 15.7575 ms、1 次新连接 |

执行器基准保留意图的持久提交，必要 SELECT 前后均为 13 次；在 broker 入口前终止，订单数为零。计时不包含外层规划、调度等待或交易所网络，不能作为实盘下单延迟。迟到成交基准使用单个已核对意图下的 2 万条合成部分成交，初始数据写入不计时；位置靠近末尾时收益较大，不表示所有迟到位置均能同样加速。原始数据保留在本地 `output/performance-audit-2/implemented-measurements.json`。

固定功能代码在 Windows Python 3.11 完成后端全量 **1435 项：1419 通过、16 项 Linux 专用检查跳过**（366.321 秒，退出码 0）；命令为 `python -m unittest discover -s tests -t . -q`，设置 UTF-8 环境。回归覆盖连接复用与独立回滚、跨线程锁隔离、迟到累计精度、通知批量原子性、慢普通读取期间循环执行、取消失败后的任务归属、快照撤销、模式有效期、杠杆资格、发单间隔、格式化筛选以及计时采集失败不影响交易异常。此前全量轮次的两处旧时间夹具已修正，以上为修正后的完整重跑结果。

前端全部 **121 项测试**、类型检查、lint 和 Node 22.23.2 生产构建通过。执行质量展开区新增账本访问锁等待、连接建立、守卫、预算、签名、HTTP 调用和响应解析计时；只记录阶段时间，不记录请求正文、签名或密钥。旧记录与缺失计时显示“—”，真实零耗时保留。HTTP 调用耗时包含连接处理，数据库耗时属于准备阶段，提交前聚合与细分阶段有包含关系，不能重复相加。隔离模拟页面已检查 1440 像素桌面与 390 像素手机，无横向溢出；新数据与旧记录展示均正确，控制台只有既有 favicon 404。浏览器截图中的阶段数值为展示样例，不属于上表性能实测。模拟服务与浏览器已停止，未连接实盘账户、下单、发送通知或部署服务器。

Codex Security 对固定补丁 `56d2b7e..9076871` 的 **12 个变更源文件**及直接边界完成独立架构映射和逐文件复核，未发现可报告问题。扫描 `a66caf4b-4058-4041-9c64-2b838e10b3e6` 已封存、回读并通过规范验证：coverage 为 complete，deferred 和 findings 均为零，六字段架构模型内容保持原样。两个 openQuestions 记录未检查的实际部署配置和既有外部并发协议假设；基线循环快照已并行 GET，本次没有在线验证交易所 nonce 或 HTTPX 外部兼容契约。报告保留 `_StoreSnapshot` 旧只读注释与新增可写提醒批处理用法的差异，实际网页只读仍由连接标志强制。以上为本次补丁复核，不代表全仓或部署环境审计。插件记录两项关联任务累计 **10,411,743 tokens**，含 **9,612,288 缓存输入 tokens**；这是关联任务累计口径，包含本任务实施过程，不是此次安全复核的独立用量。本地报告为 `output/performance-audit-2/security-report.md`。

## 重复状态与本地计算简化（2026-09-16）

滚动成交统计按账户账本版本及有效时间区间缓存；插入、删除、更新与版本变更同事务提交，跨 Store 写入、迟到成交、回滚、旧 WAL 快照、UTC 换日、窗口释放及未来成交进入均不能沿用错误结果。成本和最近 100 笔历史共享一次配对计算，后台每次仍重新检查补账状态。删除重复的账户日汇总表，按最多三个品种汇总精确求和；升级保留原始成交与品种汇总。

同一深度版本复用不可变快照及精确累计档位，每次规划继续检查时效、序列撤销和发单前资格。账户实际杠杆读取保留热缓存校验，省去整个可变账户副本。每账户调度状态归并到一份记录，去掉未被读取的等待标记；循环展示只保留当前诊断与阶段元数据，持久进度通过统一投影读取。公开唤醒信号去掉未消费的数量与价差字段，原数量和精确边界预筛继续执行。

固定功能提交 `7532ec9` 在 Windows Python 3.11 完成后端全量 **1416 项：1400 通过、16 项 Linux 专用检查跳过**（406.187 秒，退出码 0）。命令为 `python -m unittest discover -s tests -t . -q`，并设置 UTF-8 环境。回归覆盖缓存失效、旧快照、精确边界、账户调度及报告隔离；发单前意图、索引与事件仍在同一写事务落盘。

前端保留单个在途刷新，请求当前账户的历史明细；版本相同时复用已收到的列表，切换立即取消旧请求并重读，账户删除和退出清理缓存。移除 52 个未引用 UI 组件、1 个未使用 hook，以及 106 个安装依赖项；保留仍使用的组件与构建依赖。前端全部 **119 项测试**、类型检查、包含剩余组件的 lint 和 Node 22.23.2 生产构建通过。

本机 Windows Python 3.11、单账户 20000 笔合成成交，中位数对比：强制重新计算滚动量 **61.3108 ms**，同版本重复读取 **2.0011 ms**；强制重算历史报表 **703.4609 ms**，复用 **1.9514 ms**。买卖各 1000 档构建精确累计深度 **6.2638 ms**，同版读取约 **0.0001 ms**。冷计算各取 5 次（深度 30 次），复用取 50–100 次；报表复用仍读取当前额度和补账状态。两账户示例（仅一个有 100 条展示历史）状态 JSON 从 **90426 bytes** 降至未变化轮次 **40188 bytes**，约减少 **55.6%**；首轮仍需传完整历史。以上为本地缓存命中收益，不包含真实网络延迟，也不保证每次成交变化后的冷计算同样加速。

隔离模拟服务实测两账户 100 / 200 USD1 历史往返切换正确，连续轮询请求携带对应版本并保留显示；1440 像素桌面和 390 像素手机无页面横向溢出。控制台只有既有 favicon 404。截图和基准记录保留在本地 `output/playwright/performance-review/`。未连接实盘账户、发单、发送通知或部署服务器。

Codex Security 对固定补丁 `81019d2..7532ec9` 的 73 个变更源文件及直接边界完成独立架构映射和逐文件复核：20 个运行或构建文件未发现可报告问题，53 个删除文件已从基线核对为页面不可达组件或 hook。扫描 `0b2ea0f1-45d6-47a9-9b7c-dac9c3c94eb1` 已封存并回读，coverage 为 complete，deferred 和 findings 均为零，六字段架构模型原样保留。范围为本次补丁，不代表全仓、部署环境或依赖供应链审计。插件记录四项关联任务累计 **10,587,358 tokens**，含 **10,071,296 缓存输入 tokens**；这是关联任务累计口径，不是单次安全复核的独立用量。

## 页面偶发状态超时（2026-09-16）

状态接口原先在每次刷新时扫描近 24 小时循环成交、重复计算所选品种成本，并使用与交易写入共用的数据库锁。Windows Python 3.11 本地合成单账户 50000 笔成交，改动前一次 `Engine.state()` 耗时 3.268 秒。HTTP 状态现在只读取当前账户、执行进度及已经完成的统计；单个后台任务共享计算结果，独立只读 WAL 快照不占用交易写入锁。后台计算同时运行时，20 次状态读取的中位数 3.9 毫秒、最大值 337.3 毫秒；计时不含浏览器、代理或真实服务器网络，不代表已确认用户服务器上的唯一超时原因。

新增 6 项后端回归，覆盖已鉴权状态在统计阻塞期间连续响应且不堆积任务、写入事务未提交时仍能读到一致的已提交状态、只读快照拒绝写入、后台与同步统计金额一致、旧显示统计不改变发单额度核对或执行阶段、失败后保留原时间及账户/配置隔离。首次结果使用缺失字段表示未知，不能伪装为零；过期、UTC 换日及失败明确标记，当前启停和订单状态独立读取。补齐既有 API 测试对后台资源的清理后，最终相关 17 个模块 **204 项：200 通过、4 项 Linux 专用检查跳过**（50.842 秒）。

前端全部 **115 项测试**、类型检查、lint、修改组件检查、格式检查和 Node 22.23.2 生产构建通过。隔离模拟服务完成 1440 像素桌面和 390 像素手机检查：首次统计显示未知，失败保留 201 USD1 成交量及成本并显示原统计时间，恢复后清除失败提示，无横向溢出。浏览器接口替身仅用于复现加载与失败显示，后台不阻塞由真实鉴权接口的并发回归验证。未接入实盘账户或部署服务器。

## 运行中循环账户信息更新（2026-09-15）

复现后台账户快照更新成功但 `/api/state` 仍返回策略旧快照：测试中后台余额已从 25000 更新为 30000，网页仍收到 25000。状态响应现在读取同账户当前有效、时间更新的后台快照，仓位、余额、权益及占用率共同更新；保留独立的最后显示副本，避免后台失效时退回更早读数。原始读取时间保留，过期、断线、配置不符、刷新失败或序列化期间被撤销的数据不能刷新显示资格。该变化不增加 REST 请求、不修改执行视图、不改变交易调度或持仓。

新增六项回归，覆盖后台更新直接显示、较新执行快照优先、失效后保留最后显示与时间、过期和断线等失败分支、响应准备期间撤销、账户隔离及暂停。相关 11 个模块共 **160 项：158 通过、2 项 Linux 专用检查跳过**（37.657 秒）；最后补充保留较新执行显示的处理后，账户更新与成本状态模块 **22 项通过**（6.436 秒）。未改动前端文件，因此未重复前端构建；未连接实盘账户或部署服务器。

## 循环持仓人工核对恢复（2026-09-15）

新增异常文字下方的“已核对”入口：先读取并对照持仓，确认时重新读取，按实际仓位结束旧循环跟踪；不下单、不修改杠杆，账户保持暂停，之后需手动启动。未完成批次、过期或跨账户凭据、仓位/杠杆/设置/进度变化、无效模式或权限均不可跳过。旧状态、核对结果、账户暂停状态和事件在同一事务保存，保留成交量、成本及订单历史。

后端相关五个模块共 **74 项：72 通过、2 项 Linux 专用检查跳过**（12.993 秒），其中恢复模块 **13 项全部通过**。覆盖真实模拟循环产生的异常、仅一边被人工平仓、完全空仓、重启与重复确认、并发新增批次、事务失败回滚、鉴权与来源验证。补充“启动账户”检查发现仓差的回归：修复前没有保存暂停原因，导致恢复入口缺失；修复后保存异常状态、保留循环和订单，核对后仍暂停。

前端 **114 项测试全部通过**，类型检查、lint、修改组件 lint/格式检查及 Node 22.23.2 生产构建通过。本机 Node 24 在此前构建退出时出现既有 libuv 断言，未计为成功。入口识别兼容缺少可恢复标志、标志为 false、循环异常说明和当前账户启动错误；其他账户的错误不显示此入口。隔离模拟页验证启动失败后立即出现按钮及刷新后保留；桌面及 390 像素手机页面中，按钮位于异常文字下方且无横向溢出。模拟未完成批次响应时，入口仍可见，弹窗显示具体原因且确认禁用。恢复流程保留实际多/空 6/7，本轮新增记录归零，确认后暂停，订单数仍为 0。未访问实盘账户或部署服务器。

## 循环只使用当前实际杠杆（2026-09-15）

确认现有循环执行路径只使用当前实际杠杆，不自动升降，也不使用较低档位额度代替当前档位。此次没有修改生产逻辑，仅补充部署说明和两项行为回归：当前 20x 额度缺失、为零、不足或过期时，即使 2x/5x/10x 均有充足额度，公共预筛和账户执行仍等待，零发单且不调用任何杠杆调整方法；当前 17x、旧配置仍为 2x 时，仅凭 17x 额度完成开仓，随后无公共额度仍按 17x 减仓并恢复原始不等量多空数量。

循环额度门槛、公共采样、原仓保留三个模块共 **37 项全部通过**（5.833 秒，退出码 0），含上述两项新用例。生产代码和前端未变化，因此未重复运行全量或前端构建；上一节功能版本的全量结果保留在下方。未访问真实交易接口或部署服务器。

## 循环额度倍数门槛与减仓保留原仓（2026-09-15）

功能代码提交 `37580fb`。循环开仓先检查实际杠杆的公共额度，再检查本地热深度价差；额度阈值为配置的本轮多空合计目标名义价值乘 `capacity_multiplier`，默认 1，支持 1–100 的小数。目标取名义价值上限，按每边配置时乘 2，例如每边 10000 USD1、2 倍要求公共余量至少 40000 USD1。仍需满足账户档位余量、保证金和成交量限制，实际下单量可能低于目标上限。

额度样本从请求起点计算，超过 1 秒、查询失败、缺失或实际杠杆不匹配均等待；不使用相邻杠杆替代。复用 200 毫秒目标采样和后台公共档位缓存，不在热交易路径增加 HTTP 查询。额度版本更新可唤醒尚有效的热差价机会；最终规划及意图落盘后、发单入口前再次检查，失败记为确定未发送，恢复不重发旧开仓单。减仓与补救不检查公共额度，只处理本轮新增量，完成后保持原始多空数量及杠杆。

后端专项 75 项通过（14.033 秒），包括额度先于价差、阈值精确计算、非法配置原子拒绝、实际 2x 档位、额度恢复唤醒、忙账户保留新额度机会、落盘后额度下降零发单，以及原始不等量多空仓位连续三轮循环，在公共额度为零、失败和缺失时仍减回原数量。首轮全量 1351 项有一项旧测试仅提供普通 5/10/20x 额度；补齐该测试所用 2x 额度后，市场选择及新额度模块共 24 项通过（12.926 秒）。

最终固定代码在 Windows Python 3.11 完成全量 **1351 项：1335 通过、16 项 Linux 专用检查跳过**（317.016 秒，退出码 0）。使用项目虚拟环境并设置 `PYTHONUTF8=1`、`PYTHONIOENCODING=utf-8` 运行 `python -m unittest discover -s tests -t . -q`；该轮启动后生产代码和测试均未修改。未向交易所下单、发送通知或部署服务器。

前端全部 112 项测试、类型检查、lint 与 Node 22.23.2 生产构建通过；格式整理后相关 11 项测试及修改组件 lint/格式检查通过。隔离模拟页面完成 2.5 倍保存、刷新保留和 390 像素手机检查，无横向溢出。浏览器验收曾发现 API 配置模型缺少新字段，已修正并增加 API 回归后重新验证。模拟服务和浏览器会话均已停止；临时模拟数据库及锁文件因自动审批阻止删除而保留，截图和测试日志保留在本地 output 目录。

Codex Security 对固定补丁 `2140002..37580fb` 的 9 个生产源文件及直接边界完成独立架构映射和逐文件复核，未发现可报告问题。扫描 `6b600a14-d32c-46e9-9d9b-91ed6efd5354` 已封存并回读：coverage 为 complete，9 个表面均已记录，deferred 和 findings 均为零，六字段架构模型原样保留。功能提交后的改动仅补齐测试前提和验证文档。插件记录关联两项任务累计 8,977,857 tokens，其中缓存输入 8,641,536；该数不是单次静态复核的独立成本。

## 循环标的 200 毫秒额度采样与账户余量（2026-09-15）

功能代码提交 `ec177e3`。账户及循环启用、实际杠杆就绪后，对选中标的按 200 毫秒目标开始间隔采样，同标的跨账户共享，在途请求不重叠，慢请求不补积压。非循环档位约每 2 秒更新；暂停或关闭循环恢复常规间隔。公共档位独立每 60 秒热更新，5 分钟失效，读取和更新失败不续期；每次高频采样只有一个公共 GET。用户明确保留循环开仓规则，公共采样只用于查询和展示。

前端显示账户当前杠杆余量（账户上限减多空合计持仓），复用现有账户快照，不额外查询账户接口。缺失、断线、杠杆不匹配或过期时不显示可用金额；各公共档位使用自己的采样时间。

新增 13 项后端专项测试覆盖单 GET、精确档位、冷热缓存、失效、限流、启停、实际杠杆、同标的共享、慢请求、原退避保留、每档时间和账户双向扣减。最终相关后端 132 项通过（9.521 秒）。全量最后一轮运行 1338 项：1321 通过、16 项平台检查跳过、1 项旧测试夹具失败（304.998 秒）。该夹具将模拟返回间隔缩至 10 毫秒却没有同步普通调度常量，随后补齐常量替身；修正后调度及额度相关 67 项中 66 通过、1 项 Linux 专用检查跳过（7.417 秒）。修正仅涉及测试，没有再次运行全量，不把此前失败轮次记为全量通过。

前端先运行全部 110 项测试通过，随后新增逐杠杆时效测试，最终两个相关模块 7 项通过；类型检查、lint 和 Node 22.23.2 生产构建通过。Node 24 的构建退出触发本机既有 libuv 断言，不计为成功。隔离 demo 页完成桌面及 390 像素手机目视检查，无横向溢出；断网后各账户余量均显示“账户数据过期”。临时服务已停止、模拟运行目录已清理，截图保留在本地 output/playwright/capacity-qa。

真实公共接口仅做 1 次档位更新和 3 次 XAU 5x GET，均成功，本地计入 4 权重；三次 GET 耗时约 62 / 203 / 437 毫秒。此短试读说明连接和解析可用，不构成 200 毫秒网络延迟保证。未向交易所下单，未部署服务器。

Codex Security 对 `18c770b..ec177e3` 的七个变更源文件及直接边界完成独立架构映射和逐文件复核，未记录可报告问题。扫描 `ed1d4411-bcec-40c2-ba73-1919d6319f0d` 已封存；回读发现规范报告仍保留早期“等待架构和发现归档”的 deferred 项，因此 coverage 显示 partial，虽七个源文件均已有 no_issue_found 结果。保留这一产物限制，不将其宣称为规范完整覆盖报告。插件用量统计为 4,899,162 tokens（包含 4,597,760 缓存输入 tokens，按关联任务累计统计）。后续提交只补齐文档与上述测试夹具，不再修改已复核的源代码。

## 循环保留原仓位与按品种统计（2026-09-15）

每账户可选 `XAUUSD1`、`SPCXUSD1`、`CLUSD1` 中一个品种，沿用该品种交易所实际杠杆。开仓前冻结原多空数量，在扣除已有仓位占用后的额度内等量增加多空；平仓与部分成交补救仅处理本轮增量，完成后回到原数量。原仓位可以单边或不等量；外部数量或杠杆变化会暂停核对。交易所合并持仓后的开仓均价、已实现盈亏和费用仍按其记账规则变化。

新增 16 项专项测试通过，覆盖连续循环、原单边/不等量仓位、部分开仓回补、部分平仓和重启、外部改仓与改杠杆、提交回调中配置变化、原仓位扣减额度、三品种实际杠杆，以及交错和迟到成交、历史数据库迁移、按品种补账/限额/成本。UTC 当日和滚动 24 小时成交量、成本均按所选品种读取；历史成交记录保留所有品种，各行累计值按自身品种计算。旧数据库首次启动从已有成交自动重建品种日汇总。

前端相关 67 项测试、类型检查、修改文件 lint/格式检查及 Node 22.23.2 生产构建通过。隔离模拟页面分别完成三品种真实模拟循环，确认切换 XAU 至 CL 后额度与成本切换、其他品种统计保留；桌面与 390 像素手机页面已目视检查，无横向溢出。构建使用 Node 22；本机 Node 24 在构建退出阶段触发 libuv 断言，未计为成功。

最终代码完成 Windows Python 3.11 全量 **1325 项：1309 通过、16 项 Linux 专用检查跳过**（264.376 秒，退出码 0）。该轮启动后生产代码和测试未再修改；统一设置 `PYTHONUTF8=1`、`PYTHONIOENCODING=utf-8` 后运行项目虚拟环境的 `python -m unittest discover -s tests -t . -q`。未向交易所下单或部署服务器。

## 有额度开仓限定交易对（2026-09-15）

新增账户策略字段 `ordinary_symbol`，支持 `all`、`XAUUSD1`、`SPCXUSD1`、`CLUSD1`；旧账户默认全部交易对。普通调度、额度优先唤醒与执行前复核共用账户选择，指定品种无额度时等待。迁移升杠杆继续使用自身品种规则，循环保护及原批次核对、补偿继续保留；全部持仓仍计入账户风险。

项目 Python 3.11 虚拟环境相关后端回归 **136 项全部通过**（27.536 秒），其中新增 **13 项**，覆盖 API 非法值与更新原子性、运行及未决批次禁止修改、账户隔离、旧配置与重启、三种选择下的实际模拟成交、无额度及旧优先信号、提交前配置变化、迁移升杠杆、循环及部分成交补救。前端相关 **16 项测试通过**，类型检查、lint 和 **Node 22.23.2 生产构建**通过。Windows Node 24 构建退出阶段出现已有 libuv 断言，不计为成功；随后使用项目已验证的 Node 22 完成构建。独立模拟页面确认选项中文显示、保存后禁用其他品种、刷新保留选择，以及恢复全部交易对。未执行实盘订单。

## 循环账户热数据与零查询发单路径

正常循环开仓和平仓在行情触发至首次下单之间均为 **0 个 HTTP 查询**。账户与按需模式、风险字段在后台刷新，私有 WS 事件撤销旧快照，历史成交补账使用独立后台任务；循环前台、后台均移除 `openOrders`。触发线程只读本地账户、报价及完整深度，未就绪、断线、配置变化或过期时等待后台恢复。启动准备、杠杆调整、下单后的成交确认与恢复仍可查询。

热缓存保留后台请求起点的 8 秒期限、模式原始 15 秒期限以及条件档位的原始 5 秒期限；任何读取和本地估值不续期。新发布、账户事件、写操作和断线均撤销旧资格；跨越这些事件的后台结果不能发布。最终回调后及原子 FULL 落盘后、提交入口前再次校验，后者拒绝标记为确定未发送，后续恢复不得重发原单。

私有推送契约核对 [Aster User Data Streams](https://asterdex.github.io/aster-api-website/futures-v3/user-data-streams/)及[V3 鉴权说明](https://asterdex.github.io/aster-api-website/futures-v3/general-info/#api-authentication-type)：推送只含变化项，不含足够字段来替代完整风险快照，也没有文档化的账户序号/REST 同步桥接。因此推送用于撤销和唤醒，后台完整快照负责重建基线，缺行不推断零值。获取与续期 listenKey 均复用现有签名和预算。

完整流水线使用真实 LiveBroker、MarketData、EIP-712 签名与隔离 HTTPX 响应：先后台预热，再清空计数并触发行情，验证双向开仓和平仓至提交入口均无私有或公共 HTTP。同时验证后台不请求挂单、历史缺账只本地等待、模式/仓位/资金/额度错误仍拦截，以及报价和深度缺失不触发 REST 回退。初轮热缓存、私有流、后台调度、执行器和流水线共 **143 项全部通过**（11.242 秒）。

并发复核发现历史补账会与完成回调写入同一终态批次，现改为事务内只合并补账字段，保留交易状态、控制字段和较新的观测、分页与已确认成交；迟到失败不能撤销成功补账，也不能把完成批次复活。相关 **67 项全部通过**（145.876 秒），含 4 个确定性并发/终态用例和 2 万条成交的分页中断恢复。

首版固定补丁完成 Windows Python 3.11 全量 **1295 项：1279 通过、16 项 Linux 专用检查跳过**（450.135 秒，退出码 0）。独立审查随后确认停止时的有界等待可能让旧连接回调迟到，误把重启后的新热缓存置为断线；最终修正为每次启动绑定独立身份，在轻量锁内核验身份并更新缓存，停止先撤销旧身份。新增确定性用例确认旧连接的状态及事件不影响新缓存，当前连接仍正常撤销资格。此修正仅涉及私有流回调与该测试；最终源码的热代理、后台调度、执行、完整流水线、热缓存与私有流 **83 项全部通过**（19.158 秒，退出码 0）。全量结果属于最后回调修正前的固定版本，不宣称最后版本重新跑过全量。

最终固定补丁经独立审查未发现未解决阻断；前端耗时标签更新为“账户热数据读取”，相关 **13 项测试通过**，差异检查通过。未调用真实交易接口、部署服务器或发送通知；无需新增配置、依赖或数据库迁移。

## 删除循环发单前冗余查询（上一阶段记录）

此阶段常规路径使用一次 `accountWithJoinMargin`、一次所选品种 `openOrders` 和一次 `multiAssetsMargin`，同轮快照完成规划与提交前本地复核，删除第二轮读取。双向模式从账户持仓方向判断，全部仓位复用本地有效标记价格；固定实际杠杆下的空仓开仓额度取双边账户 `maxNotional` 较小值。保证金模式在执行器准备本轮快照时强制刷新；缺行情或缺额度字段时才补查。旧版的两轮/8 GET 记录保留在下一节，已被本节实现替代。

字段契约核对 [Aster Futures V3 Account Information](https://asterdex.github.io/aster-api-website/futures-v3/account%26trades/)：账户 `positions` 包括空仓，单向为 `BOTH`，双向为 `LONG` / `SHORT`，浮盈亏字段为 `unrealizedProfit`，包含 `maxNotional`，没有标记价、强平价和当前多资产保证金模式。测试共享响应改为这些官方字段，避免借用 `positionRisk` 字段错误证明接口可替代。未知强平价输出 null，前端保持未知显示。

完整 WS 到提交入口的模拟测试以正常开仓和平仓均只发 **3 个签名 GET** 为最终断言，较前版 8 个减少 **62.5%**。测试在提交入口停止，没有实际下单 POST。覆盖本轮异常持仓、余额不足、零额度、挂单、单向和多资产保证金模式拦截；历史同步期间变化由后置的唯一账户读取捕获。双边平仓方向、同数量估计、七段非负耗时与总耗时一致、缺失观测时钟均保留验证。未承诺能发现最后账户响应之后发生的外部交易。

初版定向回归：完整链路、WS 调度、循环与普通品种并行、执行、规划和原普通账户解析共 **124 项全部通过**（32.649 秒）；独立单轮执行新增 **22 项**，连同原观测与原子落盘共 **51 项全部通过**（16.452 秒）。之后独立复核发现账户响应不含多资产保证金模式，缓存 false 会漏掉外部切换；因此发单准备改为每轮强制刷新该模式，最终为 3 个查询，不将初版 2 个查询宣称为交付结果。仅基础快照的展示/交易后核对仍可使用原 15 秒缓存。最终测试同时验证已有新鲜 false 缓存、外部实际切换为 true 时也不保存意图、不下单。后续完整回归结果在本节补充。

补修前首轮完整回归 **1212 项：1196 通过、16 项 Linux 专用检查跳过**（313.115 秒，退出码 0）。独立复核还发现条件补查的档位额度需要保留原始 5 秒有效期：现将档位请求起点随内部快照保留至最终 `require_fresh`，该字段不输出到页面。确定性用例验证档位缓存年龄 4.9 秒时可用、5.1 秒时拒绝且不续期；另在最终回调内跨过此边界，确认即使本轮账户只经过 0.2 秒，也不创建订单意图、不发单。账户与并发查询共 **36 项通过**（0.261 秒），执行器该新增用例单独通过（0.520 秒）。

最终固定代码完成定向 **74 项全部通过**（7.530 秒），覆盖完整 WS 发单路径、信号调度、单轮执行、官方账户响应与并发补查。随后 Windows Python 3.11 全量 **1214 项：1198 通过、16 项 Linux 专用检查跳过**（346.610 秒，退出码 0）；该轮开始后生产代码和测试未再修改。前端唯一改动为强平价可空类型，类型检查及 lint 均通过。未运行实盘订单或部署服务器；无需新增配置、依赖或数据库迁移。

## 循环发单准备延迟优化（上一阶段记录）

前后两轮账户核验保留，每轮独立的私有 GET 最多 6 个并发执行，全部请求结束后才解析、检查或抛出错误。提交前仍强制刷新双向与保证金模式，保留资产和仓位一致性、选中品种挂单、快照时效、共享预算和最终完整深度风控。新批次、成交量索引和执行事件合并为一次原子 FULL 提交，成功后才发单；展示估计复用最终风控计划按实际委托数量算出的双边金额。

新增并发查询 **14 项全部通过**（0.524 秒），使用真实 EIP-712 签名和隔离的 HTTPX MockTransport，覆盖签名恢复与 nonce 唯一性、最多六请求并发、普通快照仍串行、缓存及强制刷新请求数、缓存按请求开始计龄、等待期间风控档位缓存过期、整轮超时、失败后在途请求全部结束、429 退避、三个预算上下文的原权限及异常后不泄漏。旧测试中一处按串行请求顺序断言的实现依赖已改为比较同一组请求，随后新增模块全部通过。

执行优化新增 **12 项**，连同已有成交质量测试 **29 项全部通过**（14.376 秒）；原循环执行、诊断、额外保证金、滚动执行及按品种并行共 **69 项全部通过**（44.787 秒）。直接在 `broker.submit` 边界读取数据库，确认批次、索引和事件均已在一次 FULL 提交中持久化；覆盖索引/事件写入和提交前异常整体回滚、零发单、重启不重发原单、已有 ID 不被覆盖、最终计划不重复扫描深度、平仓方向映射、身份或数量不符显示未知，以及观测失败不改变真实交易校验。

新增完整 WS 接收至提交入口的集成测试 **4 项全部通过**（1.903 秒）。两种读取方式执行相同 8 个签名 GET，资产、仓位和挂单各查询两次，生成相同方向与数量的双边委托；第二轮仓位或账户模式改变均零发单，模式异常暂停账户。开仓和平仓分项耗时均完整、非负且能与总耗时一致核对；缺失观测时钟不改变委托。测试在提交入口停止，未发送任何下单 POST。

受控对照使用已预热模式和档位缓存、每个 GET 固定 **40 ms** 模拟响应延迟，串行和并行各运行 3 次。WS 接收至提交入口的中位耗时从 **484 ms 降至 250 ms，减少 48.35%**；每次均为两轮共 8 个 GET，返回时没有遗留在途请求，双边方向和数量相同。串行对照复用原普通顺序快照，双方使用同一份优化后的本地准备，因此该对照主要隔离账户读取并行化的收益。这里包含本机签名、调度、规划与 SQLite 落盘，不包含真实交易所网络或撮合；不能据此承诺用户实盘 409 ms 会降到固定数值。

前端新增 **3 项测试**，合计 **106 项通过**，类型检查、lint 与 Node 22 生产构建通过。桌面 1600px 与手机 390px 验收通过：折叠明细中显示七项发单准备耗时，旧记录和缺失、非有限、负数或字符串值显示未知，真实零值保留；原三段总耗时没有重复求和，未改变右侧卡片或账户切换行为。

首轮 Windows Python 3.11 完整回归共 **1162 项：1146 项通过、16 项 Linux 专用测试跳过**（313.976 秒，退出码 0）。此轮完成后，独立审查发现本轮新取的风控档位可能先于慢账户查询返回，并在使用前超过 5 秒；首轮通过不代表这一未覆盖边界安全。

已将风控档位的使用时检查统一应用于旧缓存和本轮新取结果，过期才额外读取一次，保留整轮快照原来的 8 秒起点。新增确定性测试先复现旧实现仍采用 1,000,000 档位上限，修复后确认改用刷新所得的 1,000 上限；同时检查总共 7 个 GET、刷新在调用线程、快照仍从最早查询开始计时。新增并发模块累计 **15 项**，连同原快照和循环升档相关共 **60 项全部通过**（5.163 秒）。

补修后的最终固定代码完成 Windows Python 3.11 全量 **1163 项：1147 项通过、16 项 Linux 专用测试跳过**（295.620 秒，退出码 0）。本轮启动后生产代码和测试未再修改。随后在相同 40 ms 模拟响应、两轮 8 个 GET 条件下重新测量 3 组，串行与并行中位耗时为 **500 ms → 313 ms，减少 37.40%**；仍无遗留在途请求，委托方向与数量一致。前后两次测量保留各自结果，不把本机调度与落盘波动解释成实盘改善保证。未执行真实下单或升级用户服务器；原一键升级即可启用，无新增依赖、配置或数据库迁移。

## 循环 WS 唤醒与成交质量记录

有效 BBO / 已同步深度更新仅发送本地唤醒信号；调度先精确预筛完整深度，再由账户任务重新核对配置、仓位、风险和提交前盘口。相同机会合并，单账户不并发，优先派发至少间隔 1 秒，忙时保留最新机会；普通任务原定到期时间继续保留。只有明确的公共价差或深度条件等待可被新机会提前唤醒，余额、保证金、成交量、接口预算及未知异常的退避继续生效，无有效 WS 时沿用原定时检查。

新增监听器 **16 项测试**，连同既有报价流、深度流和两者的市场集成回归共 **94 项全部通过**（5.476 秒）。验证新有效 BBO 与同步深度在缓存更新后、锁外通知，标记价格、重复、无效和未同步事件不通知；深度初始化回放全部成功后只通知一次并保留原始接收时间。监听器可注销，回调异常不破坏行情连接或缓存。

公共预筛新增 **24 项全部通过**（0.015 秒），覆盖参考金额价差与开仓最低可行委托/完整平仓数量的两道精确阈值、数量步长、金额范围、盘口有效期及持仓时间；候选数量不替代账户规划结果。真实调度循环新增 **20 项全部通过**，验证行情回调可提前越过普通到期等待、忙碌账户最新机会合并与 1 秒最小间隔、快速任务保留普通到期时间和其他品种优先任务，以及暂停、未决批次、额度、持仓计时、预算与未知异常不能被绕过。

独立复核另发现并修复两处唤醒延迟：快速暂停、改配置及恢复可能在两次调度之间完成，因此机会标识纳入配置版本；首次有效深度同步会保留缓冲事件的原始时间，可能早于已有 BBO，因此按来源分别保留顺序水位。同源旧事件仍拒绝，原始信号时效仍复核。三项新增测试在修复前均能复现失败；修复后调度 20、账户快速执行 9、公共预筛 24、监听器 16 共 **69 项全部通过**（4.129 秒）。

执行观测新增 **17 项全部通过**（8.479 秒），现有循环执行、诊断、额外保证金、滚动成交量执行及按品种并行共 **69 项全部通过**（18.697 秒）。真实模拟账本按同批每边委托数量对照触发深度、最后复核深度和原始双边实际均价，确定性时钟验证三段耗时分别在本地请求边界计算。覆盖平仓 BUY/SHORT 与 SELL/LONG 换边、部分开平仓及修复不补成完整原始价差、超时后确认成交但响应时间仍未知、未发送/拒绝/待确认/未知状态、重启保留已知值及旧批次缺失时间保持未知。

观测验证还确认：不额外请求深度 REST 或查询完整原订单，不重新提交原单；展示读取或写入失败不阻断正常交易，原交易校验错误仍阻止提交；账户最新记录隔离，迟到历史批次不覆盖较新批次，仅更新观测字段不改变终态或成交量同步标记。页面使用执行记录首位的概览展示三阶段，盘口、成交与响应明细默认折叠，右侧循环卡片保持简短；记录为原始双边口径，不作为含修复总成本或实盘价差改善的证明。

账户快速执行新增 **9 项全部通过**（6.463 秒），验证模拟实际开平仓及按同一数量记录三阶段、持仓计时未到不查询私有状态、过期/暂停/错品种/既有批次/成交后检查拒绝唤醒、最终深度变化零发单、外部仓位改变暂停账户、LiveBroker 整轮预算不足时零 HTTP 请求，以及最新质量记录按账户隔离并跨重启保留。快循环开仓后普通轮次仍能实际模拟成交其他品种。

前端共 **103 项测试通过**，类型检查、lint 与 Node 22 生产构建通过。桌面及 390px 浏览器验收确认三阶段、负价差、部分成交、调用失败和修复状态正确；超长精度数值无横向溢出，账户切换仅有一个对应质量组件且详情不串用。断线保留最后记录时间与数值并标记同步中断；无记录时清空比较，恢复与账户切换仍默认收起明细，键盘可展开，既有普通成交事件继续可见。

首轮 Windows Python 3.11 完整回归共 **1123 项：1107 项通过、16 项 Linux 专用测试跳过**（259.994 秒，退出码 0）。两处唤醒边界修复后的第二轮运行 **1126 项：1109 项通过、16 项跳过、1 项错误**（320.969 秒），失败为既有并发数据库初始化测试的 WAL 切换锁竞争，未将该轮记为通过。

对这一启动竞争补充有限重试：运行模式声明与迁移仍先在原事务内完成，之后仅在初始化连接上对 SQLite BUSY/LOCKED 等待，使用单一单调时钟截止时间限制为 10 秒，并确认最终日志模式为 `wal`；其他错误立即抛出。原始竞争已用真实持锁连接稳定复现，新增 **6 项测试**连同既有隔离共 **24 项全部通过**（2.192 秒），覆盖释放锁后成功、不同数据库不串行、模式声明先提交、总时限、非锁错误及模式拒绝后原数据库字节不变。

以上修复后的最终固定后端完整回归共 **1132 项：1116 项通过、16 项 Linux 专用测试跳过**（282.977 秒，退出码 0）。此轮开始后未再修改生产代码或测试。部署沿用现有一键安装与服务，无新增配置步骤；本节测试未向交易所下单或升级用户服务器。

## 循环检查记录合并与面板收紧

循环卡片移除完整检查表，只保留简短状态；执行记录中的循环检查默认折叠，展示累计次数及首次、最近时间，展开查看最新具体原因和检查明细。后台仅合并无未决批次的循环条件等待，按账户、品种和开/平仓阶段分段，数值及失败条件变化继续累计；普通交易、重要错误和操作记录仍单独保留。旧数据库自动增加可空元数据与时间索引，不删除历史事件，旧检查仅在页面进行保守的显示合并。

新增 **23 项后端测试**：存储层 **15 项全部通过**（4.045 秒），覆盖旧库重复升级、重启续计、4 个 Store 并发累计 80 次不丢失、真实事件分段、跨账户隔离、旧 ID 更新后的最新排序与损坏元数据保留；新增 **8 项引擎测试**连同现有诊断状态和按品种并行共 **25 项全部通过**（7.975 秒），覆盖循环独立/并行检查、同原因重查及数值变化、普通分支和未决批次不聚合、原有错误与暂停处理。真实模拟验证循环等待时其他品种仍能成交，认证 API 提供对应账户的最新检查详情。

最终固定后端的 Windows Python 3.11 完整回归共 **1040 项：1024 项通过、16 项 Linux 专用测试跳过**（291.997 秒，退出码 0），覆盖订单恢复、成交与成本账本、账户模式、通知和 Web API。运行后未再修改后端实现。

前端新增 **14 项检查记录测试**，合计 **93 项通过**；类型检查、lint 和 Node 22 生产构建通过。测试覆盖后台组计数与首次/最近时间、刷新时保留分组标识、旧记录交替条件及未知阶段、开/平仓冲突分段、严格标题边界、重要异常不折叠、损坏元数据保留及合并后再截取显示条数。

桌面 1600px 与手机 390px 浏览器验收通过，无横向溢出。右侧完整诊断表已移除，普通条件提示仍保留；旧版交替检查 3 条合并且展开保留最新原文。新组 130→131 次时已展开详情保持展开并更新完整数值，手动关闭后 131→132 次仍保持折叠；切换账户显示各自品种、阶段和次数，展开状态不串用。键盘可展开详情，成交、控制、补单、回执未知及认证错误仍独立显示，右侧关键异常保留完整原因。桌面和手机截图均已检查。

## 循环与普通加仓按品种并行，并恢复条件提示

循环账户的普通候选与额度优先唤醒改为只排除所选循环品种，其他品种继续按原普通策略执行。循环条件等待不再提前结束整个账户轮次；已有批次、成交后风险标记与账户异常继续先处理。两策略之间重新读取账户，普通升档入口补齐传入配置及建单前最新配置的循环品种保护。单账户仍只允许一个未完成批次，共用总保证金和查询预算；循环与迁移互斥保留。

此前循环模式隐藏了整块普通执行条件面板。本次恢复所有模式下的条件展示，将 5x、10x、20x 额度、各档占用率上限和 BBO 价差与循环品种禁用状态分开；两处启停按钮均明确为账户总开关。实际值保留，缺失、无效、过期或连接异常的数据不会标成满足；其他品种显示自己的普通策略状态。

新增 **23 项后端测试**（11 项引擎并行、12 项执行与快照），已全部通过。同账户循环持仓、价差等待、日/滚动额度等待时，其他普通品种能实际模拟成交；两类已有批次和成交后风险标记先处理；到期循环平仓不修改普通仓位，普通成交不写入循环交易量。普通升档在建单前拒绝最新配置已选入循环的品种，其他品种可升档。总模式异常或循环仓位被外部修改时仍停止账户。

最终固定后端代码完成 **91 项相关回归，全部通过**（39.369 秒，退出码 0），覆盖新增两模块、升档选择及恢复、市场优先调度、循环生命周期/诊断状态及迁移。独立复核发现交易所会省略未请求品种的零仓行：已补充完成快照的品种覆盖检查，必要时重新读取完整快照；新增“循环品种不在普通策略列表”回归确认普通成交不会因此误暂停，最终状态包含两个品种的多空信息。

首轮完整套件运行 **1016 项，1000 项通过、16 项 Linux 专用测试在 Windows 跳过**（389.577 秒，退出码 0）。该次运行在上述空仓快照补查修复前载入代码；修复及新增该回归后的最终结果以上述 91 项针对性复查为准，未将首轮全量描述为最终版本重新运行。

前端新增 **5 项条件视图测试**，合计 **79 项通过**；最终类型检查、lint 与 Node 22 生产构建通过。桌面及 390px 浏览器确认：循环 2x 不混入普通三档判断，XAU 循环禁用说明不遮盖各项结果，同账户 SPCX 与另一个账户使用自身条件；额度/账户 8 秒和 BBO 3 秒分别过期，缺失或异常值不标绿，服务器与浏览器相差一小时也按服务器时钟判断，长数值无横向溢出。两处账户启停联动；有未保存循环草稿时两处均禁止启动，恢复原值解除，另一个账户不受该草稿影响。

## 本账户循环品种禁止普通加仓

为普通双向新增开仓加入本账户所选循环品种的明确拦截，首次检查传入配置，创建持久批次前再次检查最新保存配置。保护与杠杆、公共额度、账户暂时暂停或成交量额度等待无关；关闭循环模式后解除。API 每次依据各账户保存的设置生成 `ordinary_add_blocks`，不会沿用其他账户或旧内存视图的禁用状态。该阶段原有整账户分流已由上节按品种并行取代；品种保护、其他账户同品种执行，以及既有订单恢复和减仓补救继续保留。

新增 **21 项后端测试**（9 项规则、8 项执行、4 项状态/API），全部通过；执行新增8项与既有普通及循环44项合计 **52 项通过**。测试确认 5x、10x、20x 额度充足且可生成有效普通计划时仍不发单、不创建意图；旧账户配置及检查过程中切换配置不能绕过保护；另一账户同 XAU 实际模拟加仓成功且资金持仓不混用。旧普通批次在循环配置改变后仍可核对完成，单腿部分成交只减少新增加的超额部分。在只配置循环品种的场景中，额度优先信号与旧待处理信号不能让其进入普通下单路径；API 鉴权、立即切换品种、关闭及重启后的状态均已验证。

最终完整后端套件运行 **994 项，978 项通过、16 项 Linux 专用测试在 Windows 跳过**，耗时 255.882 秒，退出码 0。前端 **74 项测试、类型检查、lint 与 Node 22 生产构建通过**。桌面及 390px 浏览器验收确认：循环账户所选品种的三档真实公共额度保留且不再显示普通加仓触发色；切到另一个普通账户时同品种恢复其自身条件；旧接口及暂停账户仍按各自循环选择显示保护；手机无横向溢出。

## 循环失败原因明细

为原有拒绝分支增加结构化诊断与数值日志，保留数量搜索、精确风控判断、额度异常优先级、账户退避和必要补救。采样与实际委托价差显示当前 bp、要求阈值和均价；最小委托失败按最低要求数量逐项说明深度、配置范围、档位、余额、预计权益与总占用，无法可靠计算的项目不判定通过。提交前额度及成交后回退原因同步细化，诊断数据只用于展示。

新增 **28 项后端测试**（15 项规划及格式、8 项执行、5 项账户/API 集成），与相关循环规划、额外保证金和成交量回归共 **119 项通过**。验证临界值及极小越界、低 Decimal 精度、非终止小数标记、满足配置最小金额的向上取整数量、深度不足不外推、多项失败同时展示、两窗口优先级、最终提交回调后额度变化、真实模拟成交金额/保证金回退及单腿补救。成功开平仓无需生成失败诊断；同一错误重查更新时间且不重复记事件，暂停、成功或新错误清除旧详情，鉴权 API 按账户隔离且轮询不更新检查时间。

首轮 Windows Python 3.11 完整回归 **972 项运行完成，956 项通过、16 项 Linux 专用检查跳过**，225.417 秒，退出码 0。之后独立只读复核发现暂停后立即恢复、下一轮检查前旧诊断可能从内存视图重新出现；已在成功启停时清除内存诊断，另新增一项该时序回归，使本次新增后端测试共 **29 项**。最终针对循环状态、启停、普通恢复、迁移、Web API 及两个成交量窗口复查 **77 项：75 项通过、2 项 Linux 专用检查跳过**，22.704 秒，退出码 0。该最后修复没有改动交易规划和执行代码。

前端 **70 项测试**、类型检查、lint 及 Node.js 22 生产构建通过。面板保留服务端全部检查项和原始数值，将未满足、未核验项目排在前面；旧接口缺少诊断时沿用原因文本，不自行补算判断或检查时间。

浏览器使用真实规划器生成的失败诊断，验证 390px 手机完整展示 13 项检查与 12 项上下文，余额与保证金两项失败置顶，长小数及 `≈`、`≥`、`≤` 原样显示且无横向溢出。缺失深度显示“—”与未核验，账户切换、成功、暂停和旧接口回退正确，页面刷新不更新旧检查时间。截图已检查，浏览器无错误或警告，临时服务与验收会话已关闭。固定后端补丁经 Codex Security 只读风险复评，启停诊断清理问题已关闭，无待修复阻塞项；本次未操作实盘或部署用户服务器。

## 每日循环共用额外 5 个百分点

每日循环的账户总保证金占用上限改为 `min(基础上限 + 5 个百分点, 100%)`，所有循环杠杆统一适用；循环自身不另设 5% 限额。开仓规划和成交后复核共用同一计算函数，原有全部仓位占用、手续费及盘口成本、可用余额、深度和成交量窗口检查保留。账户基础配置不改写，状态接口提供循环有效上限，普通加仓与迁移规则保持各自原有行为。

新增 **15 项后端测试**：7 项规划及账户状态、8 项成交后复核及恢复。与原循环规划/执行测试合计 **73 项通过**，覆盖多个杠杆可使用完整有效总上限、全部品种与多空方向共用额度、恰好等于上限、100% 封顶、极小超额回退、费用和可用余额保留、两个成交量窗口同时限制、重启后确认或只减仓回退，以及非正权益下仍可平仓和补救。旧“超上限回退”场景改为真正超过 55% 的权益值，新增原先介于 50% 和 55% 之间的成交保留验证。

Windows 完整回归首轮运行 **944 项**，耗时 279.565 秒：927 项通过、16 项 Linux 专用检查跳过，1 项旧接口测试因完整字典预期缺少新增 `risk_limits.cycle` 字段而失败。仅补齐该测试两个账户的字段预期后，普通高杠杆、迁移、循环规划/状态及执行恢复相关 **45 项复查全部通过**（14.997 秒，退出码 0）；生产代码与首轮全套运行之间未再改动。本次未修改 Linux 部署实现或执行实盘订单。

前端 **64 项测试**、类型检查、lint 及 Node.js 22 生产构建通过。浏览器验证 53% 总占用时循环上限为 55%，100% 封顶在 390px 手机正常展示；旧 API 缺少循环上限字段时显示待服务确认，不假定新上限已生效。关闭循环后风险卡恢复普通 5x 基础上限，普通高杠杆和迁移显示保持原规则。桌面与手机截图已检查。

## 循环交易成本统计

按指定 taker 费率 `0.0125%` 对全部已入账循环成交计算手续费；同账户、同批次、同品种的 BUY/SELL 实际成交数量按先后顺序配对，差价只计入较晚成交一次，可为负数。UTC 当日和滚动 24 小时分别汇总手续费、已配对差价、总成本及待配对金额，逐笔明细保留费用拆分。跨窗口成交仍提供配对上下文，未来成交不参与配对或成本汇总。未配齐和未同步标记未完整，报表读取或计算失败显示未知。

新增 **30 项后端测试**（15 项纯成本计算、10 项账本查询、5 项账户/API 集成）全部通过。覆盖部分成交、补救、同毫秒稳定排序、有利差价、精确小数、跨日归属、24 小时边界、未来记录、迟到及重复成交、账户隔离，以及读取失败不阻碍实际模拟平仓。模拟完整一轮的四笔费用与买卖实际现金差额逐项对账，重启后明细及总额一致。账本查询在只读约束下通过，1201 个近期批次不受展示条数截断，20000 个无关历史批次不被读取。独立只读复核未发现阻塞问题。

Windows Python 3.11 项目虚拟环境完整回归 `python -m unittest discover -s tests -t . -q`：**929 项运行完成，913 项通过、16 项 Linux 专用检查跳过**，耗时 245.368 秒，退出码 0。本次只新增本地账本成本报表，不修改交易执行或部署实现，未向交易所下单。

前端 **61 项测试**、类型检查、lint 和 Node.js 22 生产构建通过。浏览器模拟跨 UTC 的两笔成交：前一日只计手续费 1.25，后一日计手续费 1.25025 与差价 −2，当日总成本 −0.74975，近 24 小时总成本 0.50025；切换另一账户只显示该账户 0.625 的未配对成本。成本读取异常时三项金额显示“—”，已知成交量仍正常。桌面与 390px 手机页面均无横向溢出，临时服务和浏览器已关闭。

## 同时约束 UTC 日与滚动 24 小时成交量

现有 `daily_volume_limit` 同时用于自然日和滚动 24 小时，不新增配置字段。滚动窗口严格为 `(当前时间 − 86400 秒, 当前时间]`，使用原有成交时间索引和精确金额累加，排除未来成交，恰好满 24 小时的成交移出窗口。原有逐笔 UTC 日累计继续保留。规划器按两个窗口中较小的剩余额度预留本轮开平仓；执行器在提交前使用同一时间重读两个统计并再次检查。补账范围扩展到最近 24 小时，昨日未核实成交不因午夜日额度重置而被忽略。

UTC 零点后，滚动余量不足仍进入 `rolling_limit`；历史成交到期释放部分额度后，可按余量重新规划较小一轮。手动暂停在午夜和滚动额度释放时均保持暂停；平仓和补偿沿用已有减仓规则。界面同时展示两个窗口的统计、剩余额度及下一笔释放时间，该时间不承诺已经满足恢复条件，也不会由浏览器自行扣减历史量。

新增 **29 项后端测试**（11 项滚动账本、11 项执行前复核、7 项规划及账户集成），并更新原先“仅跨午夜即可恢复”的旧预期。完整 Windows Python 3.11 回归 `python -m unittest discover -s tests -t . -q`：**899 项运行完成，883 项通过、16 项 Linux 专用检查跳过**，233.226 秒，退出码 0。覆盖窗口首尾边界、部分释放后的缩量重开、低 Decimal 精度、历史和未来记录过滤、索引范围查询、迟到/重启/去重、昨日未同步记录阻挡开仓、提交回调期间跨日与新增成交二次复核，以及平仓/补偿不受额度统计阻挡。固定补丁经 Codex Security 只读风险评估，未发现待修复的阻塞问题。

前端 **54 项测试**、类型检查、lint 和 Node.js 22 生产构建通过。浏览器模拟响应确认新 UTC 日日累计为 0 时仍显示近 24 小时原值并等待滚动额度；滚动窗口按自己的统计时间过期，下一笔释放时不本地减量；手动暂停与账户统计隔离正确。桌面及 390px 手机没有横向溢出，长小数统计完整显示。临时浏览器与服务验收后已关闭；没有修改 Linux 部署实现或访问实盘交易接口。

## UTC 每日成交量上限与逐笔累计

新增每账户 `daily_volume_limit`，0 表示不限。开仓按实际盘口预留本轮开仓与预计平仓量，并在最后提交前再次核对当日累计；额度不足时缩小数量或进入 `daily_limit`，保持账户启用，下一 UTC 日重新检查。手动暂停不自动恢复。平仓和补救不受额度限制，价格变化导致实际总量超出预留时仍完成减仓。真实成交 ID、订单及账户归属核验后逐笔持久入账，跨日按实际成交时间归属；重启、重复核对和迟到成交不会重复累计，旧模拟成交的估算时间明确标注。

新增后端验证覆盖精确额度边界、交易所和配置最小金额、跨 UTC 日期、次日恢复、手动暂停保留、配置升级保留持仓计时、部分成交、补偿、失败重试和旧日补记。交易所查询支持分页及重启续查，使用普通请求预算，不能消耗减仓保留额度。只读补丁评估发现单次入账条数上限与大型订单分片不兼容，已改为每批 1000 笔写入；两项 20001 笔真实 SQLite 账本回归确认完整入账和中途失败重启后累计不重复，全部写入后才标记同步完成。

最终 Windows Python 3.11 项目虚拟环境运行 `python -m unittest discover -s tests -t . -q`：**870 项运行完成，854 项通过，16 项 Linux 专用检查跳过**，耗时 193.049 秒，退出码 0。本次新增 61 项后端测试：29 项账本、20 项交易适配与恢复、12 项额度规划及账户/API 集成。只读风险复评已确认分片上限问题修复；没有修改 Linux 部署实现，也未执行实盘交易接口。

前端 **48 项测试**、类型检查、lint、格式检查及 **Node.js 22.23.2 生产构建**通过。浏览器模拟响应验证 UTC 换日时保留旧数据并等待服务确认、手动暂停优先、长小数保存不丢精度、日期筛选和账户隔离；桌面与手机没有整页横向溢出。独立演示账本的真实浏览器流程保存每日上限 1000 USD1、持仓 1 秒并启动，完成一轮后显示 4 笔模拟成交，累计 **988.29136 USD1**，剩余 **11.70864 USD1**，状态为 `daily_limit` 且账户仍启用。每笔累计依次为 **247.07312 → 494.14568 → 741.21824 → 988.29136 USD1**。验收后已手动暂停该模拟账户并关闭临时服务。

## 独立多空循环

新增按账户设置固定杠杆、品种、深度参考金额、bp 阈值、单边/合计名义范围与持仓时间的循环模式。示例 `XAUUSD1 / 2x / 每边10000 USD1深度 / ≤0.1 bp / 每边0–10000 USD1 / 60秒` 经模拟账本确认能够完成开仓、计时、平仓并重复下一轮。循环默认关闭，保存不执行；与普通加仓和迁移分流。原策略的 5x/10x/20x 限制及原有查询路径保留。

新增 **68 项后端测试**：28 项精确规划、30 项执行恢复和 10 项账户/API 集成。覆盖参考与实际委托深度价差边界、3 秒过期及 WS 失效、单边/合计上下界、全部仓位占用与手续费、2x 空仓调档、外部仓位拒绝、账户隔离、暂停及计时保留、重启与重复完成、历史空冷却时间恢复、未知订单不重发、部分成交只减仓补偿、实际成交金额/风险超限回退、失败冷却以及非正权益下平仓。提交前重新取得账户快照、核对持仓并重算计划；仓位和计时与订单完成在同一事务保存。

最终 Windows Python 3.11 项目虚拟环境运行 `python -m unittest discover -s tests -t . -q`：**809 项运行完成，793 项通过，16 项 Linux 专用检查跳过**。新增前端 9 项测试，全部 **40 项前端测试**、类型检查、lint（含新增循环组件）通过；**Node.js 22.23.2 生产构建退出码为 0**。Windows Node 24 构建曾出现退出阶段 libuv 断言，该次不计为通过。全套复查另发现原有退避测试使用真实浮点时钟，偶发算出 180.00000000000023 秒而失败；该测试改用确定时钟后保留原精确断言，4 项预算测试通过，生产预算代码未改动。

Playwright 在独立演示账本中新建模拟账户，通过网页保存并读回示例参数、保持暂停，验证实际 60 秒持仓的一轮开平、暂停/恢复计时保留及持仓时参数锁定，并确认保留历史空冷却记录的账本重启后进入第二轮；切换账户验证未保存草稿互不串用。1440px 桌面与 390px 手机均无横向溢出。深度价差按自身检测时间标记 3 秒时效，不能因状态轮询刷新而视作新行情。实现阶段使用 Codex Security 补丁风险评估，分别核对集成层及执行层的账户、持久状态和订单恢复边界；这是本次改动范围的只读评估。未部署服务器、改动实盘账户配置或发送交易所订单。

## 账户模式保留上次结果

三项账户模式的显示不再随 8 秒快照时效统一退回“待核实”。严格按当前账户快照中的布尔值显示“上次核实通过”或红色“不符合要求”，缺失及非布尔值显示“尚未核验”。快照日期、时间、经过时间与过期、连接异常提示分别显示；不新增前端历史缓存或账户接口请求。时间标签使用“账户快照”，对应抓取开始时间，避免将短暂缓存的模式结果描述为独立的实时核验。原有 `fresh`、执行条件及交易后端未修改。

新增 **10 项**显示状态测试，覆盖 8 秒边界、失败结果保留、断线、缺失/非布尔字段、异常/未来时间、经过时间边界及账户切换；全部前端 **31 项测试**、类型检查、lint 与 Node.js 22 生产构建通过。Playwright 使用本地模拟响应验证过期仍保留三项通过、失败保持红色、无快照账户不串用结果，以及断线后同时提示连接与过期。1440px 桌面和 390px 手机均无页面横向溢出。浏览器错误仅为主动模拟断线产生的请求失败；未访问实盘账户或修改交易规则。

## 共享 WS 深度与迁移提交前复核

三个标的的 `@depth@100ms` 使用独立的一条组合连接。先接收并缓存增量，再取得 1,000 档 REST 初始快照，完成更新范围桥接与后续 `pu` 连续性校验后，才将不可变原始订单簿交给展示和迁移。健康连接不周期性请求 REST 深度；需要重建时同标的并发合并，每次按 20 权重计入原有共享预算，重建最小间隔 30 秒。后台每秒从同一订单簿计算 1 万 / 5 万 USD1 展示值，本地汇总不产生请求；展示仍以 15 秒为过期边界。

迁移仍仅接受 3 秒内有效深度，墙钟和单调计时共同限制年龄；断线或序号失效会撤销已返回的旧对象。提交前重新取最新深度、BBO 并结合新账户快照和额度重算，计划数量或杠杆变化时不保存开仓意图、不发送订单。金额档的显示汇总不进入交易规划；原有可用余额、四腿费用、未平 XAU 占用、额度、深度、精确风险上限和订单恢复约束继续保留。

公共请求调度预估由每分钟 660 调整为 **420 权重**，即额度监控 180、BBO REST 回退 120、深度最坏重建预估 120；迁移每轮成本预估由 360 调整为 300。初次深度预算预检只计算当轮实际可能需要的 REST 初始快照，健康流为 0；选出计划后另行预检 `fresh_modes=True` 账户快照和批量下单 5 权重，实际请求仍各自校验预算并保留监控与订单核对的独立额度。

本次新增 **43 项**离线测试，覆盖快照与增量桥接、重复乱序与序号缺口、断线重连、时间异常、有限档位范围及耗尽后的恢复、共享读取不消耗 REST 权重、并发初始快照合并、重建冷却、实际私有请求预算和提交前重规划。旧快照撤销、显示单调时钟到期、账户复核期间断线、深度变化拒单、相同合法计划继续成交、单调年龄超过 3 秒拒绝及恰好 3 秒允许均通过。最终 Windows Python 3.11 项目虚拟环境全套 **741 项运行完成，725 项通过，16 项 Linux 专用检查跳过**；专项 43 项也全部通过。原有并发测试中固定的 10 秒展示等待断言已随每秒本地汇总更新，深度阻塞不影响额度发布的行为检查保留。

另以仅公开行情连接核对三个真实标的的 WS/REST 桥接，约 9.94 秒内全部同步，启动总共使用 60 权重；当时 SPCX、XAU、CL 深度年龄分别约 0.096、0.251、0.188 秒。该检查没有账户凭据或交易请求，不代表账户权限或实际成交验证。离线连续 50 次增量更新与展示/交易读取只消耗最初一本快照的 20 权重。未部署服务器、操作实盘账户或发送通知。

## 迁移开仓共用额外 5 个百分点

迁移使用的 5x / 10x / 20x 均按“账户基础风险上限加 5 个百分点，最高 100%”检查，额外空间由整个账户共用，不按杠杆、标的或批次叠加。基础上限为 93% 时，三档迁移上限均为 98%；普通 5x 新增仍为 93%，普通 10x / 20x 沿用 98%。迁移规划保留未平 XAU 的全部占用，仍计入四腿手续费、价差和保守估值，并检查可用余额、目标 5x 额度、实际档位容量、深度时效及数量规则。

完成或回退批次时，在同一事务中保存迁移用途和成交后检查标记。即时检查和重启恢复均使用迁移上限，普通批次及旧布尔标记不会获得迁移权限；超过上限的精确微小差额仍会暂停账户。状态接口新增独立迁移上限，网页同步展示普通 5x、普通 10x / 20x 和各档迁移的适用上限。

新增 **9 项后端回归**，覆盖基础 93% / 已占用 94% 时直接使用 5x、恰好 98%、费用和价差缩批、各档共用上限、100% 封顶、普通 5x 隔离、额度与余额阻断，以及真实模拟成交和完成后崩溃恢复。相关 **102 项后端测试通过**，最终 Windows Python 3.11 `tests` 目录全套 **698 项运行完成，682 项通过，16 项 Linux 专用检查跳过**；前端 **21 项测试**、类型检查、lint 和 Node.js 22 生产构建通过。验证只使用合成输入或独立模拟账本，未部署服务器、修改实盘账户配置或发送实际订单。

## 迁移性能与演示账本隔离

针对 `746c0e9` 的 `trading/` 全部 13 个 Python 源文件完成静态安全审查，发现一项低风险、依赖启动方式的演示账本隔离问题：直接调用 `create_app(demo=True)` 时默认可能复用普通账本并免除认证。默认 CLI 演示目录和正式 Linux 服务入口不受该默认值问题影响，演示期间实盘执行仍被禁止。本次统一工厂与 CLI 的演示目录，在 HTTP 开放前原子绑定数据库用途；正式与演示模式不能互相复用，演示也不能认领来源不明的已有账本。原有数据不删除，旧未标记演示库改用新空目录。

迁移深度由重复逐档计算改为精确累计数量/金额和二分查询，继续使用 `Fraction`，不缩减候选数量网格或放宽风控。保证金汇总只在同一次规划内复用。搜索按最旧深度的剩余寿命建立单调时钟期限，每个节点检查是否到期；保留原有 8,192 节点上限和返回前深度时效复核。期限检查在节点之间协作退出，不承诺在单个节点计算中途抢占。

Windows CPython 3.11.9、本地合成输入、三次无 profile 调用中位数；计时排除夹具构造与网络。为单独比较算法，新鲜快照墙钟在对照期间固定，耗时使用真实时钟。以下 tick、step 是自洽的测试配置，未从交易所取得，不代表实际行情或实盘延迟。

| 合成场景 | 修改前 | 最终代码 | 核对结果 |
| --- | ---: | ---: | --- |
| 四侧各 1,000 档、源 tick=1e-6、目标 tick=1e-7、step=0.001、双边额度 1,200 USD1 | 1,209.9 ms | 38.3 ms | 完整计划相同，约 31.6 倍 |
| 四侧各 1,000 档、tick=0.01、step=0.001、目标盘口 10.00/10.01 | 64.7 ms | 15.9 ms | 两者均拒绝超过 5 bp 的深度价差 |

产品与固定旧基线另外核对 31 个完整计划、25 个错误输出及 9 个基准场景，结果一致；新时限行为另行验证。真实时钟下，进入时已使用 2.9 秒有效期的无解压力输入，旧代码约 1.062 秒后才结束搜索，新代码约 99.3 ms 后以深度过期退出。该样例验证及时停止无用计算，不代表旧版曾允许过期报价下单。

新增逐档独立参照测试覆盖双侧、零数量档、1,000 档深度、精确边界与微小偏移、禁止外推；时限测试覆盖任一侧较旧、墙钟冻结以及恰好到期。演示隔离测试覆盖默认和显式目录、注入 Engine、旧账本、模式冲突、同模式重启与竞争认领。用途绑定、建表、账户和通知迁移在同一事务内，回归核对错误模式启动或初始化迁移失败后原始 SQLite 文件逐字不变；正常升级保留旧账户和待核对意图。

本次新增 **27 项**测试，最终 Windows Python 3.11 `tests` 目录全套 **689 项运行完成，673 项通过，16 项 Linux 专用检查跳过**。迁移计算与最终账本隔离补丁均经独立静态复核，`git diff --check` 通过。前端未改动；未部署、操作实盘账户或发送真实通知。性能报告与固定基线、原始结果保存在本地 `runtime/optimization-bench/performance-20260912/`。

## XAU 双向仓位迁移

增加账户独立迁移开关，默认关闭，默认 5 bp / 每边 1,000 USD1 / 5% 金额误差。按本批真实数量的 3 秒内深度选择 SPCX 或 CL，强制检查 5x 额度、实际档额度、源仓原始杠杆下限、临时保证金和四腿成本。目标先成交并核对后才平 XAU；分阶段持久化，未知结果不重发，减仓修复不超过本批预算且不动目标旧仓。迁移完成后保持停止普通新增，运行中可停止迁移并暂停账户，已提交批次继续恢复。

新增 **87 项**迁移相关测试覆盖配置原子更新、额度与杠杆门槛、精确价差/金额边界、临时占用、尾批、累计差额、部分成交、未知回执、崩溃恢复、外部仓位变动、停止操作、额度唤醒及完成事务的幂等和回滚。功能实现后的 Windows Python 3.11 `tests` 目录全套 **660 项运行完成，644 项通过、16 项 Linux 专用检查跳过**；复核后补充规划耗时使源或目标深度过期、以及恰好 3 秒仍有效的 2 项检查，再运行 **87 项迁移专项全部通过**。前端 **19 项测试**、类型检查与 lint 通过，Node.js 22 生产构建通过；本机 Node.js 24 构建后退出有既有 libuv 断言，未将该次执行计为通过。

Playwright 检查 1440px 桌面与 390px 手机布局，无页面横向溢出；实测保存后保持暂停、刷新后保留参数，并在模拟账户运行 3 批后点击停止，确认账户暂停、迁移关闭且进度保留。未部署服务器，未操作实盘账户。`f63bbfd..84f1880` 的 20 个变更文件已完成安全差异复核，没有可报告的安全发现；规划结束时重查深度时效的后续补强另经独立差异核查和上述专项回归。

## 1 万 / 5 万 USD1 深度价差

本节保留深度展示初版的验证记录。市场表改为展示两档金额的买卖成交均价相对差，按每边各 10,000 / 50,000 USD1 逐档计算；金额不足时不外推，保留原 BBO 下单和策略条件逻辑。初版公共深度快照与标记价格无关，三市场每 10 秒独立 REST 采样，每次 20 权重；15 秒后标记过期，失败保留上次结果并显示状态。当前已改为共享 WS 原始深度、每秒本地汇总，详见上方“共享 WS 深度与迁移提交前复核”。手机视图继续在每个市场下直接并列显示两档深度价差。

深度计算、异常恢复、优先通道、额度提醒和请求预算相关 **75 项后端测试通过**；`tests` 目录回归通过，其中 **16 项 Linux 专用检查在 Windows 上跳过**。覆盖多档成交与最后一档部分取量、买卖单侧不足、精确金额边界、空盘、非法数值、乱序/交叉盘口、源时间、慢请求、错误恢复和独立调度。前端 **16 项测试**、类型检查、lint 通过，Node.js 22 生产构建通过。Windows Node.js 24 构建退出时仍出现已有 libuv 异常，未计为通过。

Playwright 在 1440px 桌面和 390px 手机视图检查展示，验证深度不足、过期及更新失败，并确认手机页面没有横向溢出；同时检查后端直接提供的生产静态页面。使用正式公开只读深度接口验证 XAUUSD1、SPCXUSD1、CLUSD1 的响应解析和两档金额计算，未调用任何交易或账户接口，未部署服务器。接口字段及权重依据 [Aster 官方深度接口](https://asterdex.github.io/aster-api-website/futures-v3/market-data/#order-book)。

## 杠杆统一为 5x / 10x / 20x

交易升档、开仓规划、实际执行、公共额度监控、通知和页面设置只使用三档，默认最低开仓杠杆为 5x。新配置拒绝其他档位；旧最低配置向上迁移到最近的支持档位，超过 20x 的账户暂停并要求先保存新设置。旧实际仓位及未完成订单仍按原始杠杆核对，已排队的旧档位额度通知停止发送，已送历史保留。

回归覆盖三档限制的各个写入入口、旧配置迁移与重启、历史仓位及待核对批次、额度通知迁移，并保留高档优先通道、2 秒采样、请求预算和精确风险边界。Windows Python 3.11 全套运行 **588 项，569 项通过、19 项 Linux 检查跳过**。前端 **11 项测试**、类型检查和 lint 通过；Linux Node.js 22.23.2 生产构建通过。Windows Node.js 24 构建产物生成后出现 libuv 退出错误，未将该次构建计为通过。

Linux/WSL Python 3.12.3 全套普通用户运行 588 项，其中 576 项通过、12 项安装测试要求 root 而跳过；随后以 root 补跑这 12 项，全部通过，**588 项均完成通过验证**。安装测试使用临时目录及模拟的系统命令，不操作真实服务。

Playwright 使用模拟账户接口验证菜单仅有三档、选择 10x 后提交整数 `min_open_leverage: 10`，检查桌面和手机布局。临时预览、浏览器和验证文件已清理，未部署服务器。下文保留旧版本的历史验证记录，其中的旧档位描述不代表当前支持范围。

## 飞书额度提醒使用网页策略阈值

公共额度提醒改为读取网页已保存的账户额度阈值，旧 `ASTER_CAPACITY_ALERT_THRESHOLD` 不再覆盖。按市场汇总相关账户，使用其中最低阈值严格判断，每个市场与档位只发送一条消息并列出本次额度达标的账户及阈值。暂停账户仍可接收提醒；没有账户关注的市场不回退到旧默认阈值。公开提醒只判断额度，不承诺满足全部开仓条件。

提醒去重标识绑定相关账户及阈值，发送前重新读取网页配置。提高阈值会拦住已排队或失败重试的旧消息；降低阈值后下一次有效采样使用新值，无需重启。等价的数值写法保持去重，重启沿用已保存的配置及成功记录。

新增 8 项回归覆盖 100,000 严格边界和暂停账户、旧环境覆盖失效、升高与降低网页阈值、失败重试、多个账户及市场隔离、多个达标账户合并提醒、发送过程中改变阈值，以及重启和等价数值去重。现有通知测试更新为网页配置来源，同时保留数据过期、缺失、发送失败、事务与并发检查；所有飞书请求均由模拟发送器处理。

Windows Python 3.11 相关 **45 项全部通过**；Linux/WSL Python 3.12.3 全套 **544 项全部通过，无跳过**。`git diff --check` 通过。本次未修改前端或数据库表结构，未部署服务器、发送真实飞书消息或操作实盘账户。

## 升杠杆与最低开仓杠杆分离

最低开仓杠杆仅作为新增开仓门槛，基础升档候选 4x / 5x / 10x / 20x 不再被该设置过滤；自定义配置档仍保留为额外候选。复现 CL 多空各 1.018、标记价格 98.28、4x 剩余额度 68,360，同时已有 XAU 多空各 43.516 的场景。最低设为 5x 时先升到 4x 并确认，维持原持仓；5x 额度可用后再升档、确认并开仓。模拟资金及风险上限设为足够，单独验证开仓杠杆门槛，不代表实盘账户完整状态。

新增 4 项回归在旧实现下全部失败，覆盖上述 CL 流程、最低设为 125x 时仍可逐步升到各基础档但不新增仓位、低于开仓门槛的升档计入请求预算，以及待核对升档重启后不重复请求、不提前开仓。更新原 7x 配置测试，验证依次确认 4x、5x、7x 后才开仓，并保留跳过无额度档位、严格额度阈值和禁止降杠杆检查。

Linux/WSL Python 3.12.3 首轮运行 536 项，原有 532 项均通过，新增 1 项因模拟账户仍使用默认 50% 风险上限而无法进入预期开仓步骤。修正该测试的风险配置为 90% 后，Linux 相关 **81 项全部通过**，包含全部 4 项新增回归；Windows Python 3.11 新增 **4 项全部通过**。生产逻辑在这次修正中未变。`git diff --check` 通过；未修改前端、部署服务器或操作实盘账户。

## 10x / 20x 共用额外风险空间

实际开仓杠杆确认为 10x 或 20x 时，账户基础风险上限额外增加 5 个百分点，最高 100%；其他杠杆使用基础上限。只持有 XAU 且占用恰好为 90% 时，SPCX 或 CL 的 10x / 20x 批次可使用剩余空间，预测成交后最多 95%。占用计算仍累计整个 USD1 账户的全部仓位，额外空间不会按标的、杠杆档或批次累加。完成及补偿结束时原子保存本批标的和杠杆，成交后及重启恢复时核对该标的实际杠杆；旧布尔检查标记保留基础上限。控制台同步显示基础上限、高杠杆上限及当前标的适用条件。

新增 **14 项回归**，覆盖 XAU 单独占用 90% 时新开 SPCX / CL、恰好达到 95%、非 10x / 20x 不获额外空间、最高 100%、高精度边界、两边手续费和价差、余额/深度/额度/最低批次限制、跨标的连续成交不叠加、杠杆回执尚未实际确认时禁止开仓、未完成及已完成批次重启恢复、低杠杆批次不得继承其他仓位的额外空间、实际杠杆变化、缺失持仓信息保留核对、事务失败回滚、账户间设置隔离和连续加仓汇总结束条件。原有 360 个固定种子组合场景按本批实际杠杆分别验证 50% / 55% 的精确边界，保留余额、步长、对冲数量及预测与实成交比较。

最终 Linux/WSL Python 3.12.3 全套 **532 项全部通过，无跳过**；Windows Python 3.11 新增 **14 项全部通过**。控制台 Node.js 22.23.2 的类型检查、静态检查、**11 项前端测试**与生产构建均通过；`git diff --check` 通过。本次未部署服务器或操作实盘账户。

## 风险到达上限时的杠杆限流恢复

复现一个独立于市场排序的缺陷：风险占用恰好达到 50%、XAU 已有 50,000 USD1 名义持仓且更高档额度满足条件时，升杠杆请求返回 HTTP 429，旧代码仍保留待核对意图；退避结束后实际杠杆未变，核对超时导致账户暂停。点击“重新核对”仅再次查询实际杠杆，不能恢复已拒绝的请求。

现在杠杆接口的 HTTP 429 / 418、限流代码 `-1003` / `-1015` 以及没有明确未知执行代码的 WAF 403 会结束本次意图，保留等待时长，在退避后重新检查账户、额度和升档目标。403 的 `Retry-After` 同样进入共享请求退避。HTTP 状态贯穿异常、意图持久化和超时提示，HTML 或空响应不再丢失关键状态；不保存网关原始内容。分类依据 [Aster HTTP 状态说明](https://asterdex.github.io/aster-api-website/futures-v3/general-info/)和[错误代码说明](https://asterdex.github.io/aster-api-website/futures-v3/error-codes/)。HTTP 408 / 5xx、`-1006` / `-1007` 等未知结果仍保持待核对，不重复提交；该拒绝分类仅用于杠杆调整。

新增 **4 项回归**，涵盖多种 HTTP/错误码与非 JSON 响应、等待期不发送新请求、风险 50% 时拒绝后重新评估并确认实际升档至 4x 后占用降至 25%、未知结果超时暂停及人工调整后的核对。最终 Linux/WSL Python 3.12.3 全套 **518 项全部通过，无跳过**；最后修改后的 Windows Python 3.11 相关 **53 项全部通过**；`git diff --check` 通过。验证使用模拟 HTTP 响应，未操作实盘账户或部署服务器。旧版本已覆盖的首次错误无法从历史记录恢复，因此不能认定历史故障一定由限流触发；人工调整后的“已确认”也不代表原请求自动成功。

## 已有较大 XAU 持仓场景补充复核

将问题场景更正为 XAU 已有约 50,000 USD1 多空合计名义持仓，而非空仓。构造 XAU / SPCX / CL 分别持仓 50,000 / 10,000 / 1,000 USD1、XAU 目标档公开额度 1,984,153 USD1、轮换从 SPCX 开始的模拟账户。测试资金设为充足，以单独验证市场选择；这些数值不代表完整实盘账户快照。

从 `c55e265` 提取原 `rank_candidates` 并替换到同一测试环境后，三个有仓回归均按预期失败：XAU 2x 可升 4x、4x 无额度时可升 5x、XAU 已为 5x 应优先加仓。旧排序会保留不同杠杆档位的轮换先后，因此已有较大 XAU 持仓并不能阻止 SPCX 2x 先执行；旧代码对已有持仓本来就会检查升档，空仓分支不解释这个场景。

使用 `5d928ac` 中的新排序，上述三个场景通过，并验证升档后先确认、在实际档位加仓一批、随后检查下一档。另通过模拟实盘 HTTP 响应验证已有 50,000 USD1 XAU 的遗留 2x→4x / 5x 调整，在实际杠杆为 5x 时完成核对，不重复提交或自动启动暂停账户。本次新增 **4 项回归**，Windows Python 3.11 相关 **15 项全部通过**，`git diff --check` 通过；仅补充测试和记录，无新的生产逻辑变更，也未操作实盘账户。历史“杠杆变更尚未确认”缺少当时调整记录和返回信息，仍不能判定其具体原因。

## 自动升杠杆、跨市场优先级与实盘调整核对

修复空仓时因当前档仍有额度而跳过升杠杆的问题，并将对应调度成本计入升档路径。无论空仓还是已有持仓，都检查高于当前杠杆的最低可用档位；保留升档确认后先尝试开仓一批、再检查下一档的顺序。跨市场按本次可用开仓杠杆从高到低选择，同档再比较精确 BBO 相对价差，完全相同时保留轮换顺序。覆盖 XAU 5x 优先于 SPCX 2x，以及 XAU 2x 可升至 4x 或跳过无额度的 4x 升至 5x。

实盘杠杆接口返回有明确拒绝含义的错误码时结束本次调整意图，等待重新评估，不再将确定拒绝永久当作结果未知。网络、服务端或处理结果未知错误继续保留待核对状态，不重复提交。保存有效调整回执与原始提交错误码，最终仍以新读取的账户实际杠杆确认；未确认提示包含标的、原杠杆、目标、实际值和已知错误码。接口与错误分类依据 [Aster 账户接口](https://asterdex.github.io/aster-api-website/futures-v3/account%26trades/)和[错误码](https://asterdex.github.io/aster-api-website/futures-v3/error-codes/)。

新增 **11 项回归测试**，覆盖市场选择、调度预算及通过 `LiveBroker` 和模拟 HTTP 传输执行的拒绝、未知结果、异常回执、重启恢复与实际杠杆确认。旧版本遗留的 XAU 2x→4x / 5x 待核对记录，在新账户快照确认实际为 5x 时可完成核对，不重发调整，也不自动启动已暂停账户。已有执行和成交后风控测试仅调整市场额度前提，以继续覆盖原有行为断言。

最终 Linux/WSL Python 3.12.3 全套 **510 项全部通过，无跳过**；Windows Python 3.11 相关 **62 项全部通过**；`git diff --check` 通过。本次未修改前端，未进行真实账户操作或服务器部署。截图中的历史未确认记录缺少实际请求与响应，无法据此确定当时的交易所返回原因；上述结果验证的是代码路径和恢复行为。

## 公共报价 WS 优先与 REST 回退

保留 `MarketData.book()` / `Book` 接口，通过一条公共连接共享三个标的的 `bookTicker` 和 `markPrice@1s`。买卖盘口的事件时间、事务时间以及标记价格事件时间分别校验，要求年龄在 [-1, 3] 秒内；缓存同时使用单调时钟约束寿命。标记价格消息的 `T` 是下次资金费时间，不参与报价时效。字段依据 [Aster 官方行情流](https://asterdex.github.io/aster-api-website/futures-v3/websocket-market-streams/)。

健康 WS 报价不产生 REST 请求或权重；缺少任一侧、异常、过期或断线时回退完整 REST 报价，沿用一秒共享缓存与预算检查。WS 数据不写入 REST 缓存，缺少的 WS 字段也不与 REST 混拼。恢复后的 WS 读取可以直接绕过正在进行的慢 REST 请求；REST 并发补采仍合并为一组两次请求。现有账户调度和预算预检继续保守预留回退成本。

新增 **42 项回归**：27 项流解析、时间边界、重复乱序、墙钟回拨、并发、重连退避和线程启动/停止测试，15 项零请求/零权重、REST 回退、缓存隔离及服务生命周期集成测试。独立复核发现并修复了慢 REST 阻塞健康 WS、过期旧事件误清新缓存、线程启动失败后不能安全关闭三项问题。最终 Linux/WSL Python 3.12.3 全套 **499 项全部通过，无跳过**；Windows Python 3.11 相关测试共 122 项，121 项通过，1 项既有 Linux 符号链接测试按平台跳过。两环境 `pip check` 与 `git diff --check` 通过。依赖固定为支持 Python 3.10 的 `websockets==16.0`，另含同步 SOCKS 代理支持 `python-socks==3.1.1`。

匿名只读连接实际收到三个标的全部六路行情，并通过新 `PublicQuoteStream` 取得三份满足时效检查的 `Book`；关闭耗时约 78 毫秒，接收线程正常退出。该检查只验证公共行情，不代表实盘交易验收。本轮未进行真实下单、飞书投递或服务器部署。

## 修复项复核

对 `fd685b5` 逐项复查，确认 1 项前端新回归和 2 处异常恢复缺口：

- 收到 401 暂停轮询后，显式刷新也被跳过，另一标签页更新登录会话仍无法恢复本页。现在手动刷新可重新验证会话，仍为 401 时继续暂停后台轮询，不恢复旧响应。
- 模拟账本写盘失败后保持内存一致，但执行器把确定没有提交的订单或杠杆调整继续当作远端未知结果等待。第二腿仅失败一次也可能留下单腿仓位，120 秒后转入需关注状态且重试不能收尾。现在恢复时重读模拟持久账本，只对确认缺失的模拟结果结束等待，保留已提交成交并执行必要补偿；账本读取失败不等于未成交，提交成功后抛错也按持久化回执处理。缺失补偿订单的次数只退还一次，失败的杠杆调整结束当前意图后允许重新计划。实盘查询失败仍不能推断未成交。
- 升级回滚期间再次收到终止信号会打断恢复。现在回滚开始后忽略 HUP、INT、TERM，保留第一次退出码；隔离验证覆盖首次与再次信号的全部 9 种组合。

新增 8 项模拟恢复端到端测试、1 项连续信号回归和 2 项前端会话恢复测试；相关失败场景均先在补修前复现。最终 Linux/WSL Python 3.12.3 全套 **457 项全部通过，无跳过**；前端 **11 项测试**、类型检查、lint 和 Linux Node 22.23.2 生产构建通过。原有未知订单/杠杆不得重发的断言保留，并额外使用 LiveBroker mock 验证实盘仍保守等待；只执行模拟交易和隔离安装测试，未操作真实账户、通知或服务器。

请求预算和时间戳修复未发现其他需修改的回归。未确认权重的保守叠加可能提前触发短暂预算等待；跨窗口与到期后能恢复，不会永久耗尽预算。请求开始时间参与时效检查后，慢网络下可用交易与提醒窗口会缩短，这是保留原有时效限制的预期行为。

## 代码审查与异常路径修复

本轮修复 7 项可复现问题，未调整格式或交易策略参数：

| 问题 | 修复与验证 |
| --- | --- |
| 额度读取和后续盘口读取耗时被排除在缓存年龄之外 | 从额度请求开始计时，提醒和交易缓存共用时间戳；慢请求及未来缓存不能绕过时效检查 |
| 模拟账户逐标的读取行情后重新生成时间戳 | 从读取开始计时，累计耗时超过 8 秒的账户快照正常失效 |
| 模拟订单或升杠杆写盘失败后仍在内存中生效 | 先提交候选账本，再更新内存；第二腿提交失败时只保留已持久化的第一腿 |
| 乱序用量回报漏计超时、无响应头和公共接口请求 | 未被确认的请求权重继续叠加到同 IP 用量回报；覆盖同分钟和跨分钟回报 |
| 前端轮询重叠、旧响应覆盖操作后状态或重新显示退出前数据 | 同时只保留一个有效状态请求，操作前取消旧读取，退出及 401 清除账户状态；读请求和操作请求均有超时处理，操作结果不确定时重新读取，不自动重发操作 |
| 升级切换后收到终止信号跳过回滚 | HUP、INT、TERM 均进入现有回滚流程；在隔离安装环境逐项验证旧版本、服务单元与运行状态恢复 |
| 飞书限流被当成 Aster 全局限流 | 只延迟该档位通知重试，其他档位和市场继续采样；Aster 429 仍保持全局退避 |

新增 13 项 Python 回归与 6 项前端回归。Linux/WSL Python 3.12.3 以 root 运行全套 **448 项全部通过，无跳过**，安装测试使用隔离目录和模拟系统命令。前端 Node 22.23.2 的生产构建、类型检查及 **9 项测试通过**；lint 和 `git diff --check` 通过。模拟 20 次额外刷新触发时，慢请求仍只发出一次状态读取，并验证取消后的旧响应不能覆盖新状态或释放新请求的执行槽。

`npm audit --json` 报告 0 项已知漏洞；`pip-audit -r requirements.lock --disable-pip --no-deps` 报告未发现已知漏洞。结论对应本次漏洞库检查。API 认证、CSRF、签名、订单恢复、进程锁、风控计算与 Store 持久化复核未发现其他需要修改的可证实问题。

Windows Node 24.15.0 的构建生成产物后发生 libuv 退出断言，因此不计为成功构建；上面的成功构建在项目 Linux 运行环境完成。本轮未进行真实下单、飞书投递或服务器部署。

## 移除两项账户查询与固定最低批次金额

新增 14 项测试，Linux Python 3.12.3 共 **435 项通过**：普通用户完成 425 项，另以 root 在隔离安装环境完成 10 项安装器测试。前端类型检查、lint 和生产构建通过。

模拟三标的账户读取确认不再调用 `/fapi/v3/openOrders` 或 `/fapi/v3/commissionRate`；首次快照 7 次请求、73 权重，缓存有效的下一次快照只读取余额与持仓、10 权重。外部挂单状态返回 `null`，每个标的固定使用 0.04% 手续费预估，旧手续费缓存不参与计算。单标的升档流程保持两次完整账户读取，权重由 242 降为 142；本地预算预检与调度成本同步下调，原有总预算、保留额度和快照时效约束不变。

新开仓每批每边至少 500 USD1，计划计算和提交前均按标记价格精确检查。覆盖恰好 500、数量取整后不足、超过 Decimal 默认精度的微小差额、交易所更高下限、余额与风险不足，以及旧配置上限低于 500 时保留配置并阻止新开仓。已存在的小额批次可以继续核对，部分成交后不足 500 的必要补偿仍可执行。既有成交、重启恢复、0.1% 多空容差、BBO 选择及成交后风控测试继续通过。本次未升级用户服务器。

## 同档市场按 BBO 价差选择

新增 21 项测试，Linux Python 3.12.3 全套 **421 项通过**。验证同一开仓档位优先选择相对价差较低的市场、使用精确值而非显示舍入值、价差相同时轮换，以及下一批重新比较行情。不同档位保持原轮换顺序；需要升档和自定义最低杠杆的市场按目标档位比较，确认实际升档后才开仓。

覆盖额度恰好等于阈值、冷却、盘口深度不足、账户容量不足、无效或过期报价的跳过；比较期间额度下降、目标杠杆变化或报价过期时，执行前复查并尝试其他有效候选。每个候选在本轮只读取一次盘口，复用公共额度缓存；本地预算不足或交易所要求退避时整轮停止，保留等待时长。既有升档、批次核对、成交后风险检查和请求预算回归通过。本次仅修改后端选择逻辑及说明，未操作用户服务器。

## 请求预算窗口与重复读取优化

新增 27 项测试，Linux Python 3.12.3 全套 **400 项通过**。单标的模拟升档流程在缓存有效时，调整前完整账户读取从 3 次降为 2 次，权重从 352 降为 242；该结果不代表全部流量固定减少同一比例。验证同账户刚完成的模式核验快照仅能复用一次，过期、其他快照和未强制核验模式的快照会重新读取，当前实际杠杆更高、模式错误或存在其他标的挂单时仍阻止调整。完整成交批次和成交后风控的既有回归继续通过。

预算测试覆盖有效响应时间对齐分钟边界、同分钟较低计数、上一分钟迟到回包、跨窗口在途请求、缺少用量回报的公共请求与未知结果保守计数、无效时间头，以及 429、网络和签名失败后在途记录清理。真实限流退避不随分钟重置解除；低预算时不会到达 HTTP 传输层。模拟剩余 260 权重的场景使用缓存费用预检后可以完成所需 242 权重的升档，普通请求仍不超过 1,500 的上限。

八个并发读取同一标的的调用合并为一组两次报价请求；另覆盖一秒到期、交易所时间过期、网络耗时计入缓存年龄、刷新失败不返回旧数据。预算等待按等待状态记录并限制日志频率，实际 Aster 限流仍显示错误。前端类型检查、lint 和 Linux 生产构建通过。

另对 `GET /fapi/v3/time` 做了一次无认证只读检查，确认实际返回 `Date` 与 `X-MBX-USED-WEIGHT-1M`，没有访问用户账户。请求权重与同 IP 计数依据为 [Aster V3 通用说明](https://asterdex.github.io/aster-api-website/futures-v3/general-info/)和[账户接口](https://asterdex.github.io/aster-api-website/futures-v3/account%26trades/)。本次未下单、发送飞书消息或升级用户服务器。

## 账户保证金比率与总名义持仓金额

新增 8 项指标测试，Linux Python 3.12.3 全套 **373 项通过**。验证维持保证金比率与原占用率分别计算、全部 USD1 仓位按标记价格计入总名义金额、多空不抵消、实际杠杆变化不改变名义金额，并覆盖空仓、零或负权益、跨标的与超过默认 Decimal 精度的金额累加。两账户轮询与重复读取状态的测试确认数据独立，新增指标不产生额外账户、行情或下单请求，原开仓风险判断保持不变。

前端类型检查、lint 和 Linux 生产构建通过。本地演示服务通过 HTTP 检查，账户快照返回比率与总名义金额；演示数据不来自用户账户。本次未向交易所下单或升级用户服务器。

## 可配置风险约束与最低开仓杠杆

新增 20 项账户配置、迁移和执行测试，Linux Python 3.12.3 全套 **365 项通过**。验证默认 50% / 4x、风险上限 30% / 70% / 100%、最低杠杆 1–125 的边界和非标准 7x 档位、旧账户补齐默认值且保留原风险上限，以及多账户配置隔离和重启持久化。API 验证暂停与未完成批次门槛、旧两字段请求和新字段单独修改、非法输入不部分保存，以及科学记数法比例展开后仍可保存。

执行测试覆盖 2x 配置下正常市价开仓、最低 10x 时拒绝 4x / 5x 新仓、7x 升档确认后继续开仓、调低最低设置不降低已有杠杆，以及历史低杠杆批次继续核对和仅补偿新增余量。精确验证不同风险上限下最后可执行数量、手续费与价差、成交后恰好等于上限及微小越界暂停。行情测试确认所有相关账户最低杠杆与实际杠杆合并到原有的一次额度采样，公共额度提醒仍仅使用原四档。

前端另有 3 组配置转换测试通过，验证百分比与比例使用十进制字符串精确转换，避免输入 33.3% 产生浮点误差，并拒绝微小超过 100% 的数值。类型检查、lint 和 Linux 生产构建通过。页面风险上限、标线、最低杠杆条件与自定义额度列均读取当前账户配置。测试使用模拟交易及通知响应；本次未升级用户服务器或下单。

## 市价开仓与补偿

新增 32 项市价执行、模拟成交和交易规则测试，Linux 全套 **345 项通过**。

新开仓和单腿补偿均改为 `MARKET`，不发送 `price`、`timeInForce` 或双向模式不接受的 `reduceOnly`。模拟 API 网关验证双向批量和单笔补偿的请求参数、方向、数量及成交后的持仓；测试还覆盖计划后价格变化、部分成交、实际均价汇总、滑点导致实际占用超限后持久暂停，以及旧 `LIMIT/FOK` 批次重启后仅按原 ID 查询、不转换或重发。补偿可执行数量低于市场最小数量时保留批次并暂停，不提交无效小单或扩大平仓范围。

市价数量使用 `LOT_SIZE` 与有效 `MARKET_LOT_SIZE` 的交集，测试验证精确步长、禁用字段、缺失与重复规则、范围冲突及最低名义金额按标记价格计算。严格数量检查仅用于三个策略标的，其他市场仍保留资产映射用于账户风险核对。模拟账本保留历史限价成交逻辑，市价仅模拟提交时可见 BBO 深度，部分成交后的余量终止；该模型不代表真实深层盘口。

接口依据为 [Aster V3 下单与批量接口](https://asterdex.github.io/aster-api-website/futures-v3/account%26trades/)和[市价过滤器说明](https://asterdex.github.io/aster-api-website/futures-v3/general-info/)。此次使用本地模拟接口，未向交易所下单或升级用户服务器。

## 增量升级与缓存

Linux 安装器测试从 2 项扩展到 **10 项，全部通过**。隔离目录中的多轮安装验证系统依赖与已下载 Node 不重复准备，Python、npm、前端产物按各自输入变化失效；后端改动不重建前端，构建修改不会污染共享 npm 缓存。另覆盖依赖安装或构建失败后重试、健康失败回滚、缺失完成标记时重建、并发安装锁，并实际降为非 root 用户读取缓存环境，确认运行权限可用。网络、服务控制与依赖安装在这些测试中使用模拟命令。

安装脚本通过 Bash 语法检查；现有 Linux 固定依赖环境通过 `pip check`。另在独立 Linux 临时目录验证真实 npm 依赖复制、类型检查、lint、生产构建及静态产物复用，均通过，临时目录已清理。此次本地复制约 691 MB 依赖耗时 8.18 秒，类型检查 3.09 秒、lint 0.49 秒、生产构建 3.39 秒；这是本机文件系统和 CPU 的结果，不代表用户服务器的升级耗时。

前端未变化的升级会直接复用静态产物，跳过 npm 安装、依赖复制和构建。首次使用新版脚本仍需建立缓存，后续升级按源码、依赖和运行时变化失效；安装日志显示各阶段与总耗时。此次未访问或升级用户服务器。

## 暂停策略恢复

新增 11 项恢复测试，Linux 全套 **305 项通过**。恢复入口改为在点击后由服务端读取并核验当前账户，避免暂停账户降频刷新与前端 8 秒快照限制冲突。测试覆盖旧快照、修复后的账户模式、即时返回最新账户视图并清除旧暂停文案，以及当前模式错误、快照过期、权益不足、无交易权限、未完成订单和实盘开关未启用时继续拒绝启动。模拟接口还确认启动请求本身不下单、不调整杠杆，序列化或账户保存失败不发布运行状态。

前端类型检查、lint 和 Linux 生产构建通过。页面低于 4x 时显示先升档的实际条件。此次未操作用户服务器，也未点击实盘启动。

## 公共额度飞书提醒恢复

新增 37 项通知与持久化测试，Linux 全套 **294 项通过**。验证三标的与四档杠杆分别触发、严格超过阈值、连续高位去重、回落后冷却重触发、重启恢复，以及旧数据库升级保留交易汇总。

集成测试核对行情方法调用次数：额度提醒直接使用原有轮询结果，没有新增 Aster 查询。提醒独立于账户和实盘开关；未配置 Webhook 或演示模式不创建公共提醒历史。发送前重新读取候选，过期、缺档、查询失败和已回落的额度不发送，失败重试使用新值；交易汇总优先，且不受公共提醒开关或阈值配置错误影响。

通知发送与数据库事务分离，不持数据库锁等待网络。测试覆盖通知在途时额度回落再升高：旧成功回执开始冷却，但不消耗新一轮触发，冷却到期后仍可提醒。入队、成功状态和事件分别在相应事务中提交，数据库故障回滚不会留下半完成状态；飞书已接收但响应丢失仍可能导致重试重复。

上述通知发送全部使用模拟响应，未向真实飞书群发送消息。部署后沿用现有机器人配置；本轮未访问或重启用户服务器。

## 请求预算与最低开仓杠杆更新

本次新增 45 项测试，Linux 全套 **257 项通过**。覆盖已有仓位和空仓在 1x、2x、3x 时禁止新增，升到 4x 后正常开仓，历史低杠杆批次继续核对与补偿。

完整批次的账户快照调用由 3 次降为 2 次，减少一次全账户读取；测试检查真实执行流程的调用次数、成交后仓位及持久化风控标记。该项减少约三分之一的快照请求，不代表全部 API 请求都减少三分之一。过期的完成快照会重新读取，未确认或补偿前快照不能用于清除风控检查。

共享预算测试覆盖普通请求的保留区、并发不超额、线程优先级隔离、同 IP 用量响应头、行内限流回执保留，以及本地未发送请求的恢复。调度测试覆盖多账户启动错峰、暂停降频、启动唤醒、未完成批次绕过普通轮询等待、冷启动与升档的较高成本。拒单或未成交的等待会逐步延长，重启后保留，成功后清除；等待期间仍可升杠杆和核对旧单。

本地预算拒绝补偿时，不消耗实际补偿次数，不在同轮反复尝试；恢复后仅继续必要补偿，不查询未发送的订单，也不重发原开仓单。此前 212 项测试及以下验证记录作为回归基线保留。

## 回归与边界

本轮新增 97 项测试。Linux Python 3.12.3 全套 212 项通过；Windows Python 3.11 分两组执行同一套 212 项，203 项通过，9 项按平台条件跳过。Windows 跳过的 Linux 权限、信号、套接字和进程场景在 Linux 完成验证。

| 范围 | 修复与验证重点 |
| --- | --- |
| 交易边界 | 数量差 0.1%、BBO 万 5、占用率 50% 的恰好值和微小越界；超过默认 Decimal 精度的数据；步长向下取整及有限小数还原 |
| 风控口径 | 全部 USD1 仓位按实际杠杆分别计入；账户和持仓接口不同步时保守计入浮亏；手续费、价差、旧仓净敞口亏损及可用余额 |
| 成交恢复 | 原单或补偿单已接受后响应丢失、写意图后崩溃、补偿结束后崩溃、零成交缺少均价、过期快照、外部挂单、极小外部仓位变化 |
| 持久化与并发 | 补偿结束与风控标记原子提交、SQLite 写入失败回滚、同账户串行与多账户独立、重复启动、锁文件别名、停止和异常退出清理 |
| 接口异常 | 重复资产和持仓、非法杠杆、分别验证两路行情时间、缓存刷新失败、HTTP 408/超时/错误 JSON、并发 nonce 和共享限流预算 |
| Web 安全 | 退出会话重放、登录换票、并发错误登录、伪造/重复 Content-Length、分块请求超限、接收超时、断流、CSRF、敏感错误输出、线程退出后的健康状态 |

`tests/test_risk_scenarios.py` 另以 17、139、7723 三个固定随机种子，各执行 120 个场景，共 360 个模拟组合。场景改变三种标的的价格、持仓数量、微小仓差、杠杆、账户余额、盘口深度和价差。每个允许成交的场景在模拟成交后再次精确检查占用率不超过 50%、可用余额非负、多空仓差不超过 0.1%。该模拟覆盖不等同于未来价格路径的保证。

## 性能

在同一台本地机器的 Linux/WSL Python 3.12.3 运行 `python -m tests.benchmark_risk`。计时不包含真实网络、交易所撮合或服务器磁盘性能。

| 场景 | 次数 | 中位数 | P95 |
| --- | ---: | ---: | ---: |
| 常规开仓计算，6 条仓位记录 | 2,000 | 0.1923 ms | 0.2485 ms |
| 极端 `1e-100` 数量步长 | 100 | 3.3382 ms | 3.8045 ms |

本地 ASGI 健康接口每轮 1,000 次请求，三轮耗时 321.98、305.88、318.11 ms。同步工作线程全部繁忙时，健康接口仍能响应；8 个慢账户同时运行时，行情任务仍能获得工作线程。空仓补全的行情查询由每方向一次改为每标的一次；账户列表从每 100 ms 查询改为每秒刷新，新增账户立即生效。

这些结果验证了本地计算和调度。账户实际刷新与交易周期仍包含 API 响应耗时和共享请求预算的退避，不能把本地计算吞吐视为实盘吞吐。

## 依赖与复现

`npm audit --json` 检查前端完整依赖树，报告已知漏洞 0；对 `requirements.lock` 中完整固定版本运行 `pip-audit --disable-pip --no-deps`，报告未发现已知漏洞。审计结论仅对应本次检查时漏洞库的记录。

```bash
.venv/bin/python -m unittest discover -q
.venv/bin/python -m tests.benchmark_risk
cd dashboard
npm audit --json
```

真实服务器的网络、交易所权限、账户费率和执行结果需要部署后另行核实。本次未部署或重启用户的服务器，也未发送飞书消息。
